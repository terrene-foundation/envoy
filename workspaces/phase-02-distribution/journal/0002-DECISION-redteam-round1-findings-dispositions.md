---
type: DECISION
date: 2026-06-08
project: phase-02-distribution
phase: analyze
topic: /analyze red-team round 1 — 2 CRIT / 9 HIGH surfaced; dispositions + fix inventory
tags: [analyze, redteam, sharding, security, sas, rendezvous, traceability]
---

# 0002 — DECISION: /analyze red-team round 1 findings + dispositions

Three parallel schema-less red-team agents (traceability, design-correctness, dependency/sequencing lenses) scrutinized the Phase-02 `/analyze` output. The brief-corrections (C1–C5, journal/0001) were independently re-verified accurate by the traceability agent. Round 1 is NOT clean — findings below. Agent IDs: traceability `a14dbca1`, correctness `a01d5df5`, dependency `a1c7858c`.

## CRITICAL

| ID        | Finding                                                                                                                                                                                                                                                                                                                                                                                                    | Disposition                                                                                                                                                                                  |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1-CRIT-1 | **WS-3 SAS binding is MITM-unsafe as written.** The QR-pairing SAS was described as "channel-bound to the Trust-Vault visible secret" — but the visible secret is a FIXED user-chosen token known before pairing; a MITM relay can forward it. A sound SAS MUST be derived from the handshake TRANSCRIPT (truncated hash of the Noise-XX/SPAKE2 transcript) so a MITM produces a different SAS → mismatch. | **FIX**: correct WS-3 research doc + Flow 2 — SAS is transcript-derived; visible secret is secondary UX only. Add the transcript-binding requirement to the threat-model device-pairing gap. |
| R1-CRIT-2 | **S6 overflows the shard budget** — packs ClassificationPolicy Tier-2 + full envelope-intersection + `chat` receive-loop (~12–15 invariants, >3–4 call-graph hops). The WS-6 deep-dive itself lists these as 3 separate shards.                                                                                                                                                                            | **FIX**: split S6 → S6a (ClassificationPolicy Tier-2), S6b (envelope-intersection + clearance mapping), S6c (`chat` receive-loop).                                                           |

## HIGH

| ID        | Finding                                                                                                                                                                                                                                                                                                                             | Disposition                                                                                                                                                                                                                          |
| --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| R1-HIGH-1 | **`grant` cross-process rendezvous unsolved by the store alone.** Grant decision is an in-process `asyncio.Future` (`grant_moment/runtime.py:305,710,743`); a store persists the row but cannot resume a waiting coroutine in another process. `grant`'s answer-in-a-later-command needs a store-poll or resident-signal mechanism. | **FIX**: split S4 → `init` (store-only) vs `grant` (rendezvous-mechanism change); add an explicit decision-rendezvous shard (replace `asyncio.Future` with store-polled/IPC wakeup).                                                 |
| R1-HIGH-2 | **EnterpriseDeploymentRecord schema+verifier+dual-sign gate (WS-4, brief:73) silently dropped** — no shard, absent from objectives matrix + flows. Real frozen-spec deliverable (`enterprise-deployment.md:15`).                                                                                                                    | **FIX**: add WS-4 shard S8b; add objectives-matrix row. (Disablement=Phase-03, pilots=Phase-04 stay out.)                                                                                                                            |
| R1-HIGH-3 | **6-language localization (WS-3, brief:64) silently dropped** — `ui-platform.md:37`, backing test exists. Non-trivial.                                                                                                                                                                                                              | **FIX**: add to S13 scope or dedicated WS-3 shard.                                                                                                                                                                                   |
| R1-HIGH-4 | **WS-6 under-sharded** — arch map collapses the deep-dive's 9 shards into 4; multi-device rebuild-from-replay dropped silently.                                                                                                                                                                                                     | **FIX**: re-expand WS-6 toward deep-dive granularity; record multi-device as explicit de-scope w/ value-anchor or shard it.                                                                                                          |
| R1-HIGH-5 | **`init`/Boundary-Conversation bootstrap shard missing** — listed as a deliverable but no shard delivers it (orphan risk).                                                                                                                                                                                                          | **FIX**: add explicit `init`/BC-bootstrap shard.                                                                                                                                                                                     |
| R1-HIGH-6 | **Missing edge: WS-4 CO-validator step-5 → WS-6 substrate.** Step-5 (`classifier_invoke`/`ensemble_aggregate`) raises `Phase02SubstrateNotWiredError` until SessionRouter exists.                                                                                                                                                   | **FIX**: split S9 → S9a (steps 1-4,6, dep S8) + S9b (step-5, dep S6a/ClassificationPolicy); OR ship step-5 deferred with a typed marker AND add a `getattr`/`importlib` adversarial sample the AST-only validator must still reject. |
| R1-HIGH-7 | **S2/S3 pack 6–7 conformance families each** (30-method adapter + 70 N-vectors in S2; 120+ E-vectors + picker + attestation in S3) against the deep-dive's "one family per shard".                                                                                                                                                  | **FIX**: shard S2/S3 by conformance family; extract picker + attestation-on-switch into their own shard.                                                                                                                             |
| R1-HIGH-8 | **Feedback-loop labels dishonest.** S5/S6 substrate-gated methods tagged "live" but have no loop until S4 lands; S3 semantic (probe-judged) families aren't deterministic-"live". Per `autonomous-execution.md` Rule 3 these must use BASE budget, not the multiplier.                                                              | **FIX**: relabel substrate-gated + semantic slices base-budget; only byte-identity slices earn the multiplier.                                                                                                                       |
| R1-HIGH-9 | **Legal-gate matrix mis-tags EC-02.1 (binary BUILD) as export-control-gated.** Building crypto-bearing binaries is not export-controlled; cross-border REDISTRIBUTION is (EAR 742.15(b)(1) = publication control, notification-email satisfiable). Mis-tag would push the whole WS-2 build matrix into the legal tail.              | **FIX**: re-scope EC-02.1 legal column to "partial (redistribution only)" per `02-ws2-distribution.md:177`.                                                                                                                          |

## MED / LOW (fold into fixes; not convergence-blocking)

- R1-MED-1: mobile screen-recording-detect + clipboard auto-clear (WS-3, brief:63) unmapped — distinct from the de-scoped _desktop_ countermeasures. Fold into S13.
- R1-MED-2: install/upgrade/rollback/`--destroy-vault`/jurisdictional-advisory surface (WS-2, brief:54) no shard. Add WS-2 shard.
- R1-MED-3: F23 threat-coverage meta-gate (T-005/T-012) untracked. Add to S6a or a verification step.
- R1-MED-4: WS-1 N3/N4 dispatch-observation seam unspecified — name the cross-runtime dispatch-observation hook. (survives)
- R1-MED-5: WS-5 k≥100 inert at <100 total users (signal-not-leak); pin whether the k-floor reads the noised or true cohort count. (survives)
- R1-MED-6: missing edges WS-1-embed→S15, WS-3-QR→S1(AKE vectors); wave-of-3 session estimate optimistic (WS-6 is a ~6-deep serial chain). Re-derive estimate post-resharding.
- R1-LOW-1: SMS mention in C2 is over-listed (SMS is Phase-04 out-of-scope, correctly unsharded) — note so `/todos` doesn't scope it.
- R1-LOW-2: WS-4 0%/0% CO-validator gate is corpus-specific (100 benign / 3 adversarial); qualify the claim.

## Net disposition

The architecture is structurally sound — acyclic DAG, correct Track-A-first/legal-tail ordering, brief-corrections gate did its job. The fixes are: (1) two correctness fixes to research docs/flows (R1-CRIT-1 SAS, R1-HIGH-1 rendezvous), (2) a re-shard of the architecture plan (R1-CRIT-2 + most HIGHs), (3) three dropped-deliverable additions, (4) the legal-matrix tag correction. None is a fatal design choice; all are framing/sizing/specification errors caught before `/todos` sizing. Fixes applied this session; re-run red-team to converge toward 0 CRIT/0 HIGH × 2 rounds (EC-6 discipline).
