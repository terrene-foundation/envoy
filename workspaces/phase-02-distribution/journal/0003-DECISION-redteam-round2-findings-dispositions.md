---
type: DECISION
date: 2026-06-08
project: phase-02-distribution
phase: analyze
topic: /analyze red-team round 2 — round-1 fixes CLOSED; 2 new HIGH + 6 MED/LOW; dispositions
tags:
  [analyze, redteam, convergence, sharding, invariant-split, orphan-criterion]
---

# 0003 — DECISION: /analyze red-team round 2 findings + dispositions

Three parallel schema-less round-2 agents: closure-parity, fresh-correctness, fresh-traceability. Agent IDs: closure `a54a0272`, correctness `a8000e70`, traceability `a958cc83`.

## Closure of round-1 findings: ALL CLOSED ✅

The closure-parity agent verified every R1 finding (2 CRIT + 9 HIGH + 6 MED + 2 LOW) landed in the corrected docs, with code anchors re-grepped (`grant_moment/runtime.py` asyncio.Future at :305/:710/:743; 5 channel modules; `Phase02SubstrateNotWiredError` at errors.py:91; no `session.py`). Round-1 fixes introduced no content regression.

## New findings (round 2): 0 CRIT / 2 HIGH / 4 MED / 2 LOW

| ID        | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Disposition                                                                                                                                                                                                                                                                                 |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R2-HIGH-1 | **Session-boundary-reset invariant (T-013) split across S5o/S6c/S4r with no owning test surface.** The reset (`session-state.md:65` — `tool_calls_made`/`goal_reconfirmation` reset at session end, the composition-aware anti-injection defense) must fire atomically with the boundary crossing: detection lives in S6c (`chat` loop), reset logic in S5o, cross-process wakeup in S4r. No shard can exercise the composed invariant — the inverse of the round-1 over-pack. | **FIX**: add thin shared shard **S5b** (session-boundary signal contract — `session_boundary_crossed` emission on all triggers) that S5o AND S6c depend on; the reset invariant is tested there. S6c carries the named integration test that a real `chat`-driven boundary fires the reset. |
| R2-HIGH-2 | **EC-02.10 (install-to-first-value <10 min mobile) orphaned** — the offline-model bundle + first-run onboarding flow (objectives deliverable; `distribution.md:59,66`, `02-ws2-distribution.md:82-89` ADR-0006 degraded-mode path) lives in NO shard. S15=build, S16=install mechanics, S3p=runtime picker — none deliver the onboarding/value flow. Same class as R1-HIGH-2/3.                                                                                                | **FIX**: add shard **S17** (first-run onboarding flow + offline-model-bundle/ADR-0006 degraded-mode + install-to-first-value <10min measured gate); dep S13+S15; add objectives EC-02.10→S17 trace.                                                                                         |
| R2-MED-1  | **SAS truncation length unspecified** — a security parameter (online-MITM blind-relay success ∝ 2^-bits). The fix corrected the binding (which value) but not the strength (how many bits).                                                                                                                                                                                                                                                                                    | **FIX**: pin a minimum residual-MITM-probability floor (≤2^-20) + rendering format in the WS-3 verdict + threat-model gap.                                                                                                                                                                  |
| R2-MED-2  | **Store-poll soundness unspecified** — missed-wakeup race (B writes between A's poll-read and sleep), poll-interval/backoff, and the `GrantMomentExpiredError` timeout audit-row contract (`runtime.py:720-726`) must survive the rewrite (zero-tolerance Rule 6). IPC-signal portability across 6 targets (musl-static breaks local IPC) is asserted-by-naming.                                                                                                               | **FIX**: S4r specifies monotonic-version re-check for missed-wakeup, preserves the timeout audit row, and recommends **store-poll as PRIMARY, IPC as per-platform optimization** (not an OR).                                                                                               |
| R2-MED-3  | **musl packed into S15** — the deep-dive's own single-HIGH-risk cell is conflated with the 5 routine targets, re-creating the over-pack the round-1 re-shard fixed.                                                                                                                                                                                                                                                                                                            | **FIX**: split S15 → S15 (5 routine targets) + **S15m** (musl mostly-static + Alpine import-smoke gate, own defer valve).                                                                                                                                                                   |
| R2-MED-4  | **DP composition / total-ε accounting unspecified** — per-counter ε does not compose to a per-user guarantee across N counters × M heartbeats; the honest claim is a total ε over a stated window. Subsumes R1-MED-5 (noised-vs-true k-floor interacts with DP noise).                                                                                                                                                                                                         | **FIX**: S11 pins the composition model + total-ε-over-window + whether the k-floor reads noised or true cohort.                                                                                                                                                                            |
| R2-MED-5  | **CO-validator 0%/0% gate split across S9a/S9b** — the dynamic-dispatch adversarial sample (AST-uncatchable) is in S9a but the ensemble that catches it (step-5) is S9b. S9a's AST-only gate cannot pass on that sample, or the "0 false-neg on 3" claim is silently narrowed.                                                                                                                                                                                                 | **FIX**: state per-shard adversarial-sample accountability — EC-02.7 "0 false-neg on 3" = 2 via AST (S9a) + 1 via ensemble (S9b); qualify the claim.                                                                                                                                        |
| R2-MED-6  | **WS-6 "~6-deep serial chain" claim contradicted by its own deps** — actually 3-deep: S4s gates a parallelizable level-2 wave (S4i/S4r/S5o/S6a/S6b/S7v); only S4g/S6c are level-3. Full-DAG critical path is depth-4 (S1→S2a→S3b→S7v), the long pole is the WS-1 conformance chain.                                                                                                                                                                                            | **FIX**: correct the dependency-graph prose; re-derive the session estimate from true critical-path depth + wave-of-3 width; note the SAME-class `runtime.py`/store merge-contention as the real serialization constraint (not dependency depth).                                           |
| R2-LOW-1  | **Shard count drift** — header "~24", footer "~26", actual enumeration **28**.                                                                                                                                                                                                                                                                                                                                                                                                 | **FIX**: state 28.                                                                                                                                                                                                                                                                          |
| R2-LOW-2  | **S7v E7 dependency may be mis-wired** — verifier needs BYTE-IDENTITY E7 (head-commitment monotonicity) for the EC-9 proof, but S7v depends on S3b (E5-E7 _semantic_/probe-judged). If E7 is byte-identity it belongs to the S2 family, not S3.                                                                                                                                                                                                                                | **FIX**: confirm E7's tier; wire S7v to the byte-identity vector source; if it reuses the full E-corpus add S3a too.                                                                                                                                                                        |

## Net disposition

Round-1 fixes all held; round 2 found 2 new HIGH (one the inverse-of-over-pack invariant split, one a fresh orphan in the same class as round-1's drops) + 6 MED/LOW, all under-specification or sizing-map omissions, none a wrong design choice. The convergence counter resets: fixes applied this session → red-team round 3 toward 0 CRIT/0 HIGH × 2 consecutive clean rounds (EC-6). The MEDs are folded into shard scopes now to prevent re-flag churn.
