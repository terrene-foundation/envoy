# Session Notes — 2026-06-15

## Where we are

Phase-02, phase **implement**. This session: **S5o-enc (session-store payload
encryption-at-rest) LANDED** (PR #110; reviewer + security-reviewer approved — no
CRIT/HIGH, 2 MEDIUM doc-fixes in-shard; CI green py3.11/py3.13). main clean + synced.
**Key-source was a user-gated fork:** spec said "vault-derived", but `grant`/`chat`
never unlock the vault → co-owner chose **keychain-gated** AES-256-GCM (no passphrase
prompt on one-shot CLIs). The session-store payload columns (`request_json` /
`resolution_json` / `state_json`) are now ciphertext on disk; local-file-read residual
CLOSED. **Next: S4g-2** (grant hardening — the only remaining in-repo buildable WS-6
shard) OR **S7v** (cross-repo — needs context-switch/authorization).

## Read first

1. `todos/active/_index.md` — scope + value-ranking (M2 row + the wave batches).
2. `todos/active/02-m2-ws6-durable-substrate.md` — § S5o-enc (✅ COMPLETE + verification), § S4g-2 (next buildable), § S6b (⚠️ Wave-3 deferral), § S7v (cross-repo).
3. `specs/session-runtime.md` § "Region encryption-at-rest (S5o-enc)" — the shipped key contract + the keychain-vs-vault deviation note.
4. `journal/0021` (S6a split) + `journal/0022` (S6c/S6d naming + #514).

## In-flight state

- This `.session-notes` write + the S5o-enc COMPLETE marker in the milestone file are the only uncommitted changes (main protected; carry them in the next docs PR).

## Outstanding ledger (forest)

| ID  | Item                                          | Value-anchor (MUST-1 source)                          | Status                          |
| --- | --------------------------------------------- | ----------------------------------------------------- | ------------------------------- |
| P1  | WS-2 distribution RELEASE (binary/brew/winget)| `ROADMAP.md:74`; brief §WS-2                          | BLOCKED — external legal gates  |
| P2  | ADR-0009 board endorsement                    | brief § external-gate blocker                         | BLOCKED — board/human gate      |
| P3  | Flagged `/todos` design Qs (N4 scoring, SPAKE2)| `todos/active/_index.md` open Qs                     | queued — SPAKE2 due before S13  |
| P4  | S8 registry persistence → DataFlow `@db.model`| `journal/0010` reviewer MED                           | OWNED — S8-persist todo         |
| P5  | WhatsApp channel PUBLIC launch                | brief §WS-3 / `ROADMAP.md:94`                         | BLOCKED — business pricing      |
| P7  | S10 security deferrals M2/L1/L2 → S11         | `journal/0011`; `threat-model.md:52`                  | queued — land with S11          |
| P9  | `sqlite_perms` WAL/SHM window                 | `journal/0012` redteam LOW                            | queued — trust-store hardening  |
| P11 | Genesis write-once TOCTOU → CAS               | PR #93 security I-1                                   | queued — with multi-device      |
| P13 | Fingerprint `name‖args` unframed seam         | S5o security-reviewer LOW (`journal/0019`)            | spec-owner — breaks N6 + kailash-rs coord |
| P14 | S6d classifier/masking/trust-verification shard| `specs/classification-policy.md` §Out-of-scope; `journal/0022` | BLOCKED — kailash-rs#514 (CLOSED but absent in 2.29.3) |
| P15 | S6b full envelope-intersection + clearance-map| `02-m2-ws6...md` §S6b; `envelope/compiler.py:296-308` | DEFERRED to Wave-3 — orphan-risk (no live consumer; lossy clearance round-trip) |
| P16 | S5o-enc residual: same-user+keychain decrypt  | `threat-model.md` § Residual (post-S5o-enc)           | documented — device-compromise class, out of local-file-read scope |

Closed this session: **P12 (session-store encryption-at-rest) — S5o-enc LANDED, PR #110.**

## Traps

- Tests: `ENVOY_KEYRING=memory uv run --no-sync pytest …` (bare `uv run` re-syncs/drops extras + pyenv 3.13 shim). CI = py3.11 + py3.13. `.python-version` is pinned 3.13.
- **Keychain CLIs: thread the resolved `ENVOY_KEYRING` backend into EVERY consumer. S5o-enc reinforced this — the SessionRouter now loads TWO keychain keys (signing + encryption) and threads ONE backend to both. Cross-process tests MUST share ONE backend instance (the OS keychain is shared in prod; `ENVOY_KEYRING=memory` mints a FRESH dict per `resolve_keyring_backend()` call → unshared). The grant CLI tests needed an explicit `cli_shares_seeder_keychain` fixture patching `envoy.cli.grant.resolve_keyring_backend` to the seeder's backend.** Catch pre-push: run cross-process CLI tests.
- WS-6 shards are SAME-class serialized (`runtime.py`/store) — NEVER worktree-parallelize within WS-6; conformance families DO (disjoint files).
- Conformance/seam "buildable" MUST be verified EMPIRICALLY — S6a/S6b/S6c/**S5o-enc** ALL drifted (SDK/spec surface present but consumer/scope wrong). S5o-enc's spec said "vault-derived" but the vault is never unlocked in `grant`/`chat` — check the actual consumer + installed binding, not the todo's claim.
- CI mypy gate is `mypy envoy/` ONLY; pyright in-editor flags ALL imports unresolvable (env artifact) — ignore.
- admin-merge: docs-only PRs path-skip tests; code PRs run the py3.11/py3.13 matrix (`gh pr checks <N> --watch` then `--admin --merge --delete-branch`).

## Open questions for the human

- **Next pick:** **S4g-2** (grant velocity-skew + 3-deep delegation-tree persistence — buildable in-repo now, S4g-1 done, ~180 LOC, ≤3 invariants) is the only remaining WS-6 shard executable from this repo. **S7v** (Rust verifier, closes forest F2→F4) lives in the SEPARATE repo `terrene-foundation/envoy-ledger-verifier` — needs a context-switch (open a session there) or a user-authorized bounded cross-repo grant (5-condition exception per `repo-scope-discipline.md`). **S6d** stays blocked on kailash-rs#514 (CLOSED but absent in installed 2.29.3 — a binding-VERSION check).
- **S6d / #514:** is `apply_read_classification` in a `kailash` >2.29.3 (→ upgrade pin) or closed-without-shipping (→ reopen, a user-gated cross-repo write)? Needs a binding-version check.
