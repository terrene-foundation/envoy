# Session Notes — 2026-06-09 (phase-02-distribution)

## Where we are

Phase-02 (Distribution + channels + runtime pluggability): `/analyze` CONVERGED → `/todos` APPROVED →
**`/implement` Wave 1 COMPLETE.** S1 (WS-1 conformance harness) + S4s (WS-6 store substrate) + S8
(WS-4 steward quorum + FV registry) implemented, reviewed (both gates APPROVE 0C/0H), and **landed on
`main`** (PR after the integration branch `feat/phase-02-wave-1`). Full suite **1758 passed / 0 failed**.

## Read first

1. `journal/0010-DECISION-implement-wave1-complete.md` — Wave-1 verification records + review verdicts + dispositions.
2. `journal/0009-DISCOVERY-implement-baseline-green-uv-env.md` — **the `uv run pytest` env requirement** (critical).
3. `todos/active/_index.md` — the 47-todo roadmap; Wave-1 status + Wave-2 next-shards.
4. `02-plans/01-architecture.md` — the 31-shard DAG + build sequence (the `/implement` source of truth).

## Next action: `/implement` Wave 2

Per the DAG (`02-plans/01-architecture.md`), Wave-2 eligible shards (deps now satisfied by Wave 1):
- **S2a** — kailash-rs-bindings adapter behind the frozen interface (deps S1). Critical path.
- **S4r** — decision-rendezvous: replace `asyncio.Future` with store-poll + monotonic-version (deps S4s). WS-6 long pole.
- **S4i** — `init`/Boundary-Conversation bootstrap (deps S4s).
- **S8e** — EnterpriseDeploymentRecord verifier (deps S8); **S9a** — CO validator steps 1-4,6 (deps S8).
- **S10** — WS-5 OHTTP key-config server + relay (root, independent).

WS-6 shards are SAME-class serialized (most touch `runtime.py`/the store) — do NOT worktree-parallelize them.

## Critical env + process facts (DON'T re-discover)

- **Tests:** `uv run pytest` or `.venv/bin/python -m pytest` (Python 3.13 `.venv` with `dataflow`/`pytest`/`nexus`).
  Bare `python -m pytest` uses the global pyenv (no `dataflow` extra) → false-red collection. Per `python-environment.md`.
- **envoy is an APPLICATION repo** (the product), NOT a PyPI BUILD repo → no `/release`; `main` + `/deploy` is the path.
- **kailash 2.13.4** in `.venv`; `nexus` is the top-level import (`from nexus import Nexus`), NOT `kailash.nexus`.
- **Worktree test env gotcha:** a fresh worktree `.venv` may lack pytest/the extras; agents should use the MAIN
  checkout's `.venv/bin/python` or `uv run` from the worktree after `uv sync`.
- **`/implement` post-merge gate:** the agents' green slice-tests are NOT sufficient — run the FULL suite via the
  complete `.venv` over the merged tree (it caught the T-020 regression from the S8 MED-1 hardening).
- **Upstream-file candidate (human-gated):** kailash `Instance-based API usage` UserWarning from `@app.handler`
  (scope-suppressed in pyproject; SDK-internal, not envoy code).

## Open / deferred (tracked)

- S8 `ContentAddressedStore` is an in-process dict (reviewer MED, deferred-by-design — Foundation registry-server
  state; DataFlow `@db.model` swap before the registry is multi-process/network-exposed).
- Flagged `/todos` open questions still pending decisions: N4 scoring metric, SPAKE2-vs-Noise (S13), store-poll
  interval (S4r), ADR-0006 degraded-mode scope (S17).
