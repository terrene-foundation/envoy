# Session Notes — 2026-06-14

## Where we are

Phase-02, phase **implement**. This session: **S6a SPLIT — structural
envelope-check engine landed** (PR #107; conformance N1/N2/N3-structural/N5
green); residual codify debt closed (#106, journal/0020); **S6c name-clash
resolved + upstream #514 verified-already-closed** (#108, journal/0022). main
clean. **Next: S6b** (envelope-intersection — buildable now).

## Read first

1. `todos/active/_index.md` — scope + value-ranking (M2 shards incl. new S6d).
2. `todos/active/02-m2-ws6-durable-substrate.md` § S6a **SCOPE AMENDMENT** (split: structural done; S6d = classifier/masking/trust deferred) + the S6b section.
3. `specs/session-runtime.md` § "Structural envelope-check engine (S6a)" — verdict schema + structural/semantic partition + posture-floor.
4. `journal/0021` (S6a SDK-gap + split rationale) + `journal/0022` (S6c→S6d resolution + #514-already-closed).
5. `todos/active/01-m1-ws1-runtime-pluggability.md` — `RS_BINDINGS_ENABLED` flag-flip gate (NOT ready).

## In-flight state

- This `.session-notes` write is the only uncommitted change (main protected; next session's first PR carries it).
- **S6d** (classifier/masking/trust surface) BLOCKED on the **kailash-rs#514 binding**: #514 is CLOSED but `apply_read_classification` is absent from the installed `kailash` 2.29.3 — a binding-VERSION question (journal/0022).

## Outstanding ledger (forest)

| ID  | Item                                          | Value-anchor (MUST-1 source)                          | Status                          |
| --- | --------------------------------------------- | ----------------------------------------------------- | ------------------------------- |
| P1  | WS-2 distribution RELEASE (binary/brew/winget)| `ROADMAP.md:74`; brief §WS-2                          | BLOCKED — external legal gates  |
| P2  | ADR-0009 board endorsement                    | brief § external-gate blocker                         | BLOCKED — board/human gate      |
| P3  | Flagged `/todos` design Qs (N4 scoring, SPAKE2)| `todos/active/_index.md` open Qs                     | queued — SPAKE2 due before S13  |
| P4  | S8 registry persistence → DataFlow `@db.model`| `journal/0010` reviewer MED                           | OWNED — S8-persist todo         |
| P5  | WhatsApp channel PUBLIC launch                | brief §WS-3 / `ROADMAP.md:94`                         | BLOCKED — business pricing      |
| P7  | S10 security deferrals M2/L1/L2 → S11         | `journal/0011`; `threat-model.md:52`                  | queued — land with S11          |
| P9  | `sqlite_perms` WAL/SHM window                  | `journal/0012` redteam LOW                            | queued — trust-store hardening  |
| P11 | Genesis write-once TOCTOU → CAS               | PR #93 security I-1                                   | queued — with multi-device      |
| P12 | Session-store encryption-at-rest — S5o-enc    | `specs/session-state.md` §Persistence; `journal/0016` | OWNED — lands WITH vault-unlock key lifecycle |
| P13 | Fingerprint `name‖args` unframed seam         | S5o security-reviewer LOW (`journal/0019`)            | spec-owner — breaks N6 + needs kailash-rs coord |
| P14 | S6d classifier/masking/trust-verification shard| `specs/classification-policy.md` §Out-of-scope; `journal/0022` | BLOCKED — kailash-rs#514 binding (CLOSED but absent in 2.29.3) |

Closed this session: none (P-ledger unchanged — S6a/S6c/#514 were shards/follow-ups, see Where we are + journal/0021–0022).

## Traps

- Tests: `uv run --no-sync pytest …` (bare `uv run` re-syncs/drops extras + pyenv 3.13 shim). CI = py3.11 + py3.13.
- WS-6 shards are SAME-class serialized (`runtime.py`/`ledger/facade.py`/store) — NEVER worktree-parallelize within WS-6; conformance families DO (disjoint files).
- Conformance/seam work MUST verify the wired surface EMPIRICALLY (`harness.resolve_runtime` → `RuntimeNotReadyError`); planning-time "buildable" drifts (journal/0021 — S6a's whole SDK-gap was missed by the todo).
- CI mypy gate is `mypy envoy/` ONLY; test files error under standalone mypy yet ship — do NOT add inconsistent type-ignores. pyright in-editor flags ALL imports unresolvable (env artifact) — ignore; mypy is the gate.
- `journal/` is integrity-guard-BLOCKED off a codify branch: the branch MUST be EXACTLY `codify/<display_id>-<date>` (no suffix) WITH an active `acquireCodifyLease` — else hard block.
- admin-merge: docs-only PRs path-skip tests (admin-merge on `tests-gate` parked); code PRs run the py3.11/py3.13 matrix (`gh pr checks <N> --watch` then `--admin --merge`).

## Open questions for the human

- **S6d / #514:** is `apply_read_classification` in a `kailash` version >2.29.3 (→ upgrade the pin) or was #514 closed-without-shipping (→ reopen/comment, a user-gated cross-repo write)? Verifying needs a binding-version check / cross-repo read grant.
- **Next pick:** S6b (recommended — buildable, `intersect_envelopes` present) vs S6d (blocked on #514) vs S4g-2 (grant hardening)?
