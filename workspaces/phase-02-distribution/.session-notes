# Session Notes — 2026-06-10

## Where we are

P8 kailash upgrade (2.13.4 → 2.29.3, whole family) DONE + `/redteam` converged
(L5, 0 CRIT/HIGH, 2 clean rounds). Three open PRs await merge: **#90** (upgrade),
**#91** (codify/journals), **#92** (pre-existing ruff lint). Upstream issue
terrene-foundation/kailash-py#1285 filed. Next: merge the PRs (CI-gated), then Wave-2 batch-2.

## Read first

1. `workspaces/kailash-upgrade/04-validate/redteam-convergence.md` — upgrade outcome + convergence + the post-convergence accuracy correction. Start here.
2. `workspaces/phase-02-distribution/journal/0012` + `0013` — upgrade DECISION + the correction (the 23 ResourceWarnings PRE-EXISTED on 2.13.4, not upgrade-introduced).
3. `todos/active/_index.md` — Wave-2 batch-2 next shards.
4. `journal/0009` — the `uv run pytest` env requirement (critical).

## In-flight state

- 3 open PRs, none merged (shared-state — left for human): #90 `chore/kailash-upgrade`, #91 `codify/jack-hong-2026-06-10` (doc-only → CI short-circuits), #92 `fix/store-ruff-lint`. #90 + #92 both touch store.py at non-overlapping lines (122/129/856 vs 297-417) → merge clean; **merge #90 first** (it carries the corrected lazy-init comment).
- Venv may be on main's deps (2.13.4) from the #92 ruff-branch work — run `uv sync --extra dev` on the upgraded branch / post-#90-merge main to restore 2.29.3.

## Outstanding ledger (forest)

| ID  | Item                                                                          | Value-anchor (MUST-1 source)                              | Status                          |
| --- | ----------------------------------------------------------------------------- | --------------------------------------------------------- | ------------------------------- |
| P1  | WS-2 distribution RELEASE (binary/brew/winget)                                | `ROADMAP.md:74`; brief §WS-2                              | BLOCKED — external legal gates  |
| P2  | ADR-0009 runtime-pluggability board endorsement                               | brief § external-gate blocker                            | BLOCKED — board/human gate      |
| P3  | Flagged `/todos` design decisions (N4 scoring, SPAKE2-vs-Noise, ADR-0006)     | `todos/active/_index.md` open Qs                         | queued                          |
| P4  | S8 registry persistence: in-proc dict → DataFlow `@db.model`                  | `journal/0010` reviewer MED                              | DEFERRED-by-design              |
| P5  | WhatsApp channel PUBLIC launch                                                | brief §WS-3 / `ROADMAP.md:94`                            | BLOCKED — business pricing gate |
| P7  | S10 security deferrals M2/L1/L2 → S11 (EC-S11.8/9/10)                          | `journal/0011`; `threat-model.md:52`                     | queued — land with S11          |
| P9  | `sqlite_perms` WAL/SHM transient-perms window on the 3 TrustStoreAdapter sibling stores | `journal/0012` redteam LOW; `trust-plane-security.md` MUST-6 (0o600) | queued — trust-store hardening  |

Closed this session: `P6` → terrene-foundation/kailash-py#1285 (AsyncLocalRuntime filed) + PR #90 (F3 `@app.handler` suppression dropped). `P8` → PR #90 + journal/0012 (upgrade landed + converged).

## Traps

- Tests: `uv run pytest` ONLY (bare `python` = global pyenv → false-red). envoy = APPLICATION repo (no `/release`; `main`+`/deploy`).
- CI: `main` ruleset EMPTY bypass — even `--admin` can't merge over red CI. #90/#92 fire the full py3.11+3.13 matrix; #91 is doc-only (short-circuits).
- `journal/` is append-only + codify-branch-only (integrity-guard) — corrections need a NEW entry (see 0013), can't edit 0012.

## Open questions for the human

- Authorize merge of #90/#91/#92 (CI-gated), merge order #90 first.
- Wave-2 batch-2 value-rank: WS-4 S8e/S9a vs M1 conformance families (S2b/c/S3a/b) vs `init` S4i.
