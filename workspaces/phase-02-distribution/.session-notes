# Session Notes — 2026-06-14

## Where we are

Phase-02, phase **implement**. Wave-2 **batch-3 COMPLETE** — S5o gate + the
M1 conformance families (N1–N6, E1–E7) merged (PRs #101–#104), codify landed
(#105). Residual codify debt (S4g-1/S5b/S3a) now **CLOSED** this session
(journal/0020) + an S6a blocker recorded (journal/0021). Next implementation
step: **S6a — but BLOCKED pending a scope decision** (see In-flight state).

## Read first

1. `todos/active/_index.md` — batch scope + value-ranking.
2. `todos/active/02-m2-ws6-durable-substrate.md` § SAME-CLASS SERIALIZATION + the S6a→S6b→S6c shards (S6a next).
3. `todos/active/01-m1-ws1-runtime-pluggability.md` — `RS_BINDINGS_ENABLED` flag-flip gate (NOT ready; most conformance still xfail-gated on S6a/S6c).
4. `specs/session-runtime.md` § SessionObservedState first-time-action gate (S5o + pattern-AST grammar).
5. `journal/0019` — batch-3 patterns; `journal/0020` — S4g-1/S5b/S3a patterns; **`journal/0021` — the S6a blocker (READ before resuming S6a)**.

## In-flight state

- This `.session-notes` + the journal/0020+0021 entries + `learning-codified.json` ride the `codify/jack-2026-06-14` PR (main protected).
- **S6a is BLOCKED on a scope decision (journal/0021).** Empirical check: the kailash 2.29.3 binding exposes NO `ClassificationPolicy`/`MaskingStrategy`/`apply_read_classification`/5-enum/`kailash.dataflow` that S6a's todo + `specs/classification-policy.md` assume; the PACT envelope/clearance surface (`intersect_envelopes`, `ClearanceSpec`, `effective_clearance`) IS present. Drift: S6a todo says it wires `classifier_invoke`/`ensemble_aggregate`, but both adapters' docstrings + `_TODO_WAVE_3` say **S6c**. 75 conformance xfails cite S6a (`envelope_check` engine), 41 cite S6c.
- **Recommendation on the table (user to decide):** SPLIT S6a — build the structural `envelope_check` engine now on the present PACT surface (unblocks most of the 75 xfails); realign masking + T-005 ensemble to S6c gated on upstream `kailash-rs#514`; correct `classification-policy.md:56-57`'s misleading "kailash-py ✅" claim. Alternatives: file the upstream gap first (human-gated), or pick a different unblocked shard (S6b `intersect_envelopes` confirmed present / S4g-2).
- Codify debt for S4g-1/S5b/S3a is **CLOSED** (journal/0020).

## Outstanding ledger (forest)

| ID  | Item                                          | Value-anchor (MUST-1 source)                          | Status                          |
| --- | --------------------------------------------- | ----------------------------------------------------- | ------------------------------- |
| P1  | WS-2 distribution RELEASE (binary/brew/winget)| `ROADMAP.md:74`; brief §WS-2                          | BLOCKED — external legal gates  |
| P2  | ADR-0009 board endorsement                    | brief § external-gate blocker                         | BLOCKED — board/human gate      |
| P3  | Flagged `/todos` design Qs (N4 scoring, SPAKE2)| `todos/active/_index.md` open Qs                     | queued — SPAKE2 due before S13  |
| P4  | S8 registry persistence → DataFlow `@db.model`| `journal/0010` reviewer MED                           | OWNED — S8-persist todo         |
| P5  | WhatsApp channel PUBLIC launch                | brief §WS-3 / `ROADMAP.md:94`                         | BLOCKED — business pricing      |
| P7  | S10 security deferrals M2/L1/L2 → S11         | `journal/0011`; `threat-model.md:52`                  | queued — land with S11          |
| P9  | `sqlite_perms` WAL/SHM window                  | `journal/0012` redteam LOW                            | queued — trust-store hardening  |
| P11 | Genesis write-once TOCTOU → CAS               | PR #93 security I-1                                   | queued — with multi-device      |
| P12 | Session-store encryption-at-rest — S5o-enc    | `specs/session-state.md` §Persistence; `journal/0016` | OWNED — lands WITH S6*/vault-unlock (S5o now populates Region-2) |
| P13 | Fingerprint `name‖args` unframed seam         | S5o security-reviewer LOW (`journal/0019`)            | spec-owner — breaks N6 + needs kailash-rs coord |

Closed this session: `P10` → terrene-foundation/kailash-py#1318 filed; receipt `journal/0018`.

## Traps

- Tests: `uv run --no-sync pytest …` (bare `uv run` re-syncs/drops extras + pyenv 3.13 shim). Fresh worktree: `UV_PYTHON=.venv/bin/python uv sync --extra dev` once. CI = py3.11 + py3.13.
- WS-6 shards are SAME-class serialized (touch `runtime.py`/`ledger/facade.py`/store) — NEVER worktree-parallelize within WS-6; conformance families DO parallelize (disjoint `tests/conformance/` files).
- Conformance agents MUST verify the wired surface EMPIRICALLY (`harness.resolve_runtime` → `RuntimeNotReadyError`); a planning-time "buildable" claim drifts (N1/N2 were fully S6a-gated this session).
- CI mypy gate is `mypy envoy/` ONLY; test files error under standalone mypy yet ship — do NOT add inconsistent type-ignores.
- `journal/` is codify-branch-only — acquire a `codify/<id>-<date>` branch first.
- admin-merge: a CI-retriggering push parks `tests-gate` as "expected" → `gh pr checks <N> --watch` then `gh pr merge --admin`.

## Open questions for the human

- **S6a scope decision (BLOCKING — see In-flight state + journal/0021):** split S6a (structural `envelope_check` now on the present PACT surface + masking/T-005 → S6c) ‖ file the upstream `kailash-rs#514` binding gap first (human-gated) ‖ pick a different unblocked shard (S6b/S4g-2)?
- If filing upstream: the cross-repo `kailash-rs` issue needs an explicit user-authorized + journaled grant per `repo-scope-discipline.md` + `upstream-issue-hygiene.md`.
