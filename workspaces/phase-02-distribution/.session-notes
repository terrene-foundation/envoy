# Session Notes — 2026-06-15

## Where we are

Phase-02, **implement**. Two shards landed this session: S5o-enc (session-store
encryption-at-rest, keychain-gated — PR #110) + S4g-2 velocity half (durable +
skew-immune velocity-raise cooling-off ratchet — PR #112). main clean + synced.
**In-repo WS-6 buildables are now EXHAUSTED** — every remaining WS-6 item is
blocked (see ledger). The next pick is a human decision (see Open questions).

## Read first

1. `todos/active/02-m2-ws6-durable-substrate.md` — § S4g-2 (✅ + S4g-2b/F12-b split), § S5o-enc (✅), § S6b/S7v (blocked).
2. `todos/active/_index.md` — milestone value-ranking (M1–M6).
3. `specs/session-runtime.md` § "Region encryption-at-rest (S5o-enc)" — shipped key contract + the keychain-vs-vault deviation note.
4. `journal/0021`+`0022` — S6a split, S6c/S6d naming, kailash-rs#514.

## Outstanding ledger (forest)

| ID  | Item | Value-anchor | Status |
| --- | ---- | ------------ | ------ |
| P1  | WS-2 distribution RELEASE | `ROADMAP.md:74`; brief §WS-2 | BLOCKED — legal gates |
| P2  | ADR-0009 board endorsement | brief §external-gate | BLOCKED — board |
| P3  | Flagged /todos design Qs (N4, SPAKE2) | `_index.md` open Qs | queued — SPAKE2 pre-S13 |
| P4  | S8 registry persistence → DataFlow | `journal/0010` | OWNED |
| P5  | WhatsApp PUBLIC launch | brief §WS-3; `ROADMAP.md:94` | BLOCKED — pricing |
| P7  | S10 security deferrals → S11 | `journal/0011`; `threat-model.md:52` | queued |
| P9  | sqlite_perms WAL/SHM window | `journal/0012` | queued |
| P11 | Genesis write-once TOCTOU → CAS | PR #93 sec I-1 | queued — multi-device |
| P13 | Fingerprint name‖args seam | `journal/0019` | spec-owner |
| P14 | S6d classifier/masking shard | `journal/0022`; classification-policy spec | BLOCKED — kailash-rs#514 |
| P15 | S6b envelope-intersection | `02-m2-ws6…md` §S6b | DEFERRED Wave-3 (orphan-risk) |
| P16 | S5o-enc same-user+keychain residual | `threat-model.md` §Residual | documented (device-compromise) |
| P17 | S4g-2b 3-deep delegation e2e | `02-m2-ws6…md` §S4g-2b; `grant-moment.md:237` | BLOCKED — F12-b (P18) |
| P18 | F12-b sync↔async cascade bridge | `journal/0042` | queued — IN-REPO buildable; unblocks P17 + EC-8 |

Closed this session: `P12` (S5o-enc) → PR #110; S4g-2 velocity half → PR #112.

## Traps

- Tests: `ENVOY_KEYRING=memory uv run --no-sync pytest …`. CI = py3.11+3.13. `.python-version` pinned 3.13.
- Keychain: thread ONE backend into EVERY consumer; SessionRouter now loads TWO keychain keys (signing + encryption). Cross-process CLI tests MUST share one backend (`ENVOY_KEYRING=memory` mints a fresh dict per `resolve_keyring_backend()` call) — see `test_grant_cli_answer_flow.py::cli_shares_seeder_keychain`.
- "Buildable" MUST be verified EMPIRICALLY — S6a/S6b/S6c/S5o-enc/S4g-2 ALL drifted (spec/SDK surface present, consumer/scope wrong). Check the real consumer, not the todo's claim.
- WS-6 shards SAME-class serialized (runtime.py/store) — never worktree-parallelize within WS-6.
- admin-merge: docs PRs path-skip the matrix; code PRs run py3.11/py3.13 (`gh pr checks <N> --watch`).

## Open questions for the human

- **Next pick — in-repo WS-6 exhausted.** Recommend **F12-b (P18)** — sync↔async cascade bridge, IN-REPO, unblocks S4g-2b + user-reachable EC-8; needs scope confirmation (not yet sharded at /todos). Alternatives: **S7v** (cross-repo — context-switch/authorization) or switch milestone (M3/M4/M5, re-rank at /todos per `wave-loop.md` G4).
- **S6d / #514:** is `apply_read_classification` in kailash >2.29.3 (→ upgrade pin) or closed-without-shipping (→ reopen, a cross-repo write)? Binding-version check.
