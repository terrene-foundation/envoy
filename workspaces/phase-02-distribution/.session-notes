# Session Notes — 2026-06-14

## Where we are

Phase-02, phase **implement**. Wave-2 **batch-3 COMPLETE** (PRs #101–#104, codify
#105); residual codify debt CLOSED (journal/0020, #106). **S6a SPLIT — structural
half DONE** (user-approved): `feat/s6a-envelope-check-structural` ships the pure
structural `envelope_check` engine; conformance **N1+N2+N3-structural+N5 flipped
green** (~46 xfails → pass). Next implementation step: **S6b** (envelope-intersection,
`intersect_envelopes` confirmed present) OR the deferred classifier/masking shard.

## Read first

1. `todos/active/_index.md` — batch scope + value-ranking.
2. `todos/active/02-m2-ws6-durable-substrate.md` § S6a **SCOPE AMENDMENT** (the split: structural done; masking/T-005/semantic deferred) + the S6b/S6c shards.
3. `todos/active/01-m1-ws1-runtime-pluggability.md` — `RS_BINDINGS_ENABLED` flag-flip gate (NOT ready; the classifier/masking + two-phase-signing + grant_moment_surface lanes still xfail-gated).
4. `specs/session-runtime.md` § "Structural envelope-check engine (S6a)" (the verdict schema + structural/semantic partition + posture-floor) + § S5o gate.
5. `journal/0019` (batch-3 patterns); `journal/0020` (S4g-1/S5b/S3a); `journal/0021` (the S6a SDK-gap + split rationale).

## In-flight state

- **S6a structural engine LANDED** (`envoy/runtime/envelope_check.py`; both adapters delegate → byte-identical). N1/N2/N3-structural/N5 conformance lanes pass; reviewer + security-reviewer both APPROVE (security MED-1 content-type fail-closed fixed in-shard; MED-2 cache-consumer assumption documented in `specs/session-runtime.md` § S6a for the S6c wiring).
- **DEFERRED (still gated on upstream `kailash-rs#514`):** the classifier/masking shard — `@classify`/`apply_read_classification`/`MaskingStrategy` + T-005 ensemble + N3-semantic. Its value-anchored AC is RETAINED verbatim in the S6a todo. The binding exposes none of that surface.
- **UNRESOLVED — S6c token collision** (flag for next `/todos` re-rank): conformance/adapters tag the classifier gate "S6c" but the milestone S6c is the `chat` loop — the deferred classifier/masking work needs its own shard id.
- **Still gated, unchanged this shard:** `grant_moment_surface` (N4 rendered text), the two-phase signing engine (E6), S6b (envelope-intersection — buildable: `intersect_envelopes` present).

## Outstanding ledger (forest)

| ID  | Item                                          | Value-anchor (MUST-1 source)                          | Status                          |
| --- | --------------------------------------------- | ----------------------------------------------------- | ------------------------------- |
| P1  | WS-2 distribution RELEASE (binary/brew/winget)| `ROADMAP.md:74`; brief §WS-2                          | BLOCKED — external legal gates  |
| P2  | ADR-0009 board endorsement                    | brief § external-gate blocker                         | BLOCKED — board/human gate      |
| P3  | Flagged `/todos` design Qs (N4 scoring, SPAKE2)| `todos/active/_index.md` open Qs                     | queued — SPAKE2 due before S13  |
| P4  | S8 registry persistence → DataFlow `@db.model`| `journal/0010` reviewer MED                           | OWNED — S8-persist todo         |
| P5  | WhatsApp channel PUBLIC launch                | brief §WS-3 / `ROADMAP.md:94`                         | BLOCKED — business pricing      |
| P7  | S10 security deferrals M2/L1/L2 → S11         | `journal/0011`; `threat-model.md:52`                  | queued — land with S11          |
| P9  | `sqlite_perms` WAL/SHM window                  | `journal/0012` redteam LOW                            | queued — trust-store hardening  |
| P11 | Genesis write-once TOCTOU → CAS               | PR #93 security I-1                                   | queued — with multi-device      |
| P12 | Session-store encryption-at-rest — S5o-enc    | `specs/session-state.md` §Persistence; `journal/0016` | OWNED — lands WITH S6*/vault-unlock (S5o now populates Region-2) |
| P13 | Fingerprint `name‖args` unframed seam         | S5o security-reviewer LOW (`journal/0019`)            | spec-owner — breaks N6 + needs kailash-rs coord |

Closed this session: `P10` → terrene-foundation/kailash-py#1318 filed; receipt `journal/0018`.

## Traps

- Tests: `uv run --no-sync pytest …` (bare `uv run` re-syncs/drops extras + pyenv 3.13 shim). Fresh worktree: `UV_PYTHON=.venv/bin/python uv sync --extra dev` once. CI = py3.11 + py3.13.
- WS-6 shards are SAME-class serialized (touch `runtime.py`/`ledger/facade.py`/store) — NEVER worktree-parallelize within WS-6; conformance families DO parallelize (disjoint `tests/conformance/` files).
- Conformance agents MUST verify the wired surface EMPIRICALLY (`harness.resolve_runtime` → `RuntimeNotReadyError`); a planning-time "buildable" claim drifts (N1/N2 were fully S6a-gated this session).
- CI mypy gate is `mypy envoy/` ONLY; test files error under standalone mypy yet ship — do NOT add inconsistent type-ignores.
- `journal/` is codify-branch-only — acquire a `codify/<id>-<date>` branch first.
- admin-merge: a CI-retriggering push parks `tests-gate` as "expected" → `gh pr checks <N> --watch` then `gh pr merge --admin`.

## Open questions for the human

- **S6a scope decision: RESOLVED 2026-06-14** — user approved the SPLIT; structural `envelope_check` engine shipped (`feat/s6a-envelope-check-structural`). Remaining open items below.
- **Deferred classifier/masking shard — file `kailash-rs#514` upstream?** The masking/T-005/semantic half is gated on the rs binding exposing `apply_read_classification`/`ClassificationPolicy`. Filing the upstream gap needs an explicit user-authorized + journaled grant per `repo-scope-discipline.md` + `upstream-issue-hygiene.md` (NOT yet filed).
- **S6c token collision** — at the next `/todos` re-rank, assign the deferred classifier/masking work its own shard id (distinct from the milestone S6c `chat` loop).
- **Next shard pick:** S6b (envelope-intersection, buildable now) vs the deferred classifier shard (blocked on #514) vs S4g-2?
