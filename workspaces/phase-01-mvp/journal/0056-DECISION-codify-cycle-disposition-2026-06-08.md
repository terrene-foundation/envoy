---
type: DECISION
date: 2026-06-08
created_at: 2026-06-08T00:00:00Z
author: co-authored
session_id: continue-from-0054-autonomize-redteam-codify
session_turn: 1
project: phase-01-mvp
topic: /codify disposition for the 2026-06-07 redteam-convergence cycle — downstream local-only codification + workflow lesson-baking + upstream-candidate surfacing
phase: codify
tags:
  [
    codify,
    downstream-coc-project,
    redteam-convergence,
    spec-accuracy,
    rate-limit-pattern,
    skip-upstream,
  ]
---

# 0056 — DECISION: /codify disposition for the redteam-convergence cycle

**Repo class:** downstream **coc-project** (`.claude/VERSION` type=`coc-project`; `.claude/` regenerated by `/sync` from loom). **Posture:** L5_DELEGATED. **Not multi-operator** (no roster; codify-lease uncontended).

## Verdict

This `/codify` produces **local-only codification** (downstream repos are pull-only per `rules/artifact-flow.md`; their `.claude/{rules,skills,agents}` are loom-regenerated, so local edits to shared artifacts are overwritten on the next `/sync`). The cycle's project knowledge was already captured during `/redteam` (journal/0055 + the round report + session notes, merged PR #84). This entry records the codify disposition + the two generic improvements surfaced for upstream.

## What was codified (local, persists)

- **`.claude/workflows/redteam-round.mjs`** (project-local reusable workflow — persists; it carries envoy-specific PREAMBLE, not a loom-synced generic artifact). Two lessons baked in so the NEXT round inherits them:
  1. **SHIPS-CLAIM ↔ REAL-IMPL CROSS-CHECK** directive in the spec-dimension PREAMBLE: every "Phase-01 ships X" spec sentence MUST grep to a non-stub impl; a ships-claim naming a deferred/typed-stub/Phase-02+ feature is a HIGH over-claim. This is the **F1-r2 class** (Round 2 caught my own data-model.md fix over-claiming the duress shadow-segment as Phase-01 when it is Phase-04). Symmetric to the existing test-location disposition #6 — finders now verify BOTH directions (test-location cites a real test for a shipped primitive; Out-of-scope names a genuinely-deferred feature).
  2. **Rate-limit FALLBACK comment** at the WAVE definition: under a live server-side throttle even waves of 3 can 429, and schema-forced StructuredOutput finders go unreliable; the reliable path is direct mechanical sweeps + schema-less SEQUENTIAL agents. (Bit both the 0053 and this cycle.)
- **`.claude/learning/learning-codified.json`** updated (this cycle's `actions_taken`; digest unchanged → digest-driven Step 1 a no-op).

## What was NOT codified (with rationale)

- **Steps 3-4 (agents/skills) + Step 7 (upstream proposal): SKIPPED.** Downstream coc-project — shared `.claude/` artifacts are loom-managed; local edits overwritten; downstream is pull-only (no proposal manifest). Same disposition as the 2026-05-24 cycle (prior `learning-codified.json`).
- **No new RULE authored** → Trust Posture Wiring (Step 6b) + self-referential-codify gate (Step 6) do not apply (the only `.claude/` edit is `workflows/redteam-round.mjs`, which is NOT on the `self-referential-codify.md` Rule-2 allowlist).

## Surfaced for upstream (human-gated per `rules/upstream-issue-hygiene.md` MUST-1)

Two generalizable COC-artifact improvements that cannot be codified locally (loom-managed) — for the user to raise via the `kailash-coc-claude-*` USE template / loom Gate-1:

1. **`rules/spec-accuracy.md`** — add the ships-claim↔real-impl cross-check to the `/redteam` audit protocol (generalizes to any frozen-spec corpus: a "ships X" claim must grep to a non-stub impl).
2. **`commands/redteam.md` / `skills/30-claude-code-patterns`** — the schema-forced-subagent rate-limit pattern + sequential-schema-less fallback (recurring across 0053 + this cycle).

## Receipts

- Convergence: journal/0055 + `04-validate/rounds-1-4-fresh-convergence-2026-06-07.md` (PR #84, merge `653541f`).
- Workflow edits verified: parses clean as an async-function body (the Workflow runtime model); `node --check` flags the top-level `return` but that is legal under the runtime's async wrapper.

## For Discussion

1. **Counterfactual:** if the ships-claim↔real-impl cross-check had been in `redteam-round.mjs` at the START of this cycle, would Round 2's F1-r2 over-claim have been caught at Round 1 (author-time) instead — i.e., does baking the check into the finder PREAMBLE actually prevent the class, or only catch it one round earlier?
2. **Data-specific:** the digest has been empty/stale since 2026-05-04 (same `digest_hash` across two codify cycles), yet both cycles produced real codification from session work. Is the `learning-digest.json` pipeline actually feeding `/codify` in this repo, or is digest-driven Step 1 structurally dead here while session-driven codification carries the load?
3. Both generic improvements are "skip_upstream" again (as in 2026-05-24). Across N downstream cycles, recurring skip_upstream entries for the SAME pattern are a signal the upstream filing never happened — should there be a standing surface (a workspace todo / a `/wrapup` prompt) that re-asks the user to file the accumulated skip_upstream items, so they don't decay?
