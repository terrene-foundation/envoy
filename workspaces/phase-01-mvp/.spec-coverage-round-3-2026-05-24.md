# Round 3 — Fresh Spec-Coverage Assertion Table (T-02-33 RT-2 closure)

**Audit date:** 2026-05-24
**Round:** 3 of /redteam (verifies Shard 3 closure of Round 2 F-1/F-2/F-3)
**Diff scope (Shard 3 only):** `git diff e89914b..2264ae2`
**Cumulative diff (T-02-33 + all 3 shards):** `git diff 641dd2d..2264ae2`
**Current main:** `2264ae2`
**Methodology:** per `skills/spec-compliance/SKILL.md` § Audit Protocol + `rules/probe-driven-verification.md` § Structural-Probes-Required + `rules/testing.md` § Audit Mode Rules. NO trust of `.test-results` or Round 2's assertion table. Every claim re-derived from scratch this round via fresh AST / grep / pytest commands.

---

## Section 1 — Round 2 finding closures (fresh re-derivation)

### R3-1 — R2-F1 atomicity AST ordering invariant

**Round 2 finding (HIGH):** F-2 mutation invariant raises interleaved BETWEEN Step 5a's posture_change append and Step 5b's envelope_edit append → on any invariant violation an orphan posture_change committed to the Ledger while paired envelope_edit never appeared.

**Closure claim:** Shard 3 commit `bf1f66f` promotes the F-2 invariant raises to PRECONDITIONS that fire BEFORE Step 5a's append. Zero Ledger entries land on any invariant violation.

**Fresh verification — AST extraction from `envoy/authorship/posture_gate.py` (HEAD = `2264ae2`):**

```text
mutate_for_posture_level call line:                  1008
PostureEnvelopeMutationInvariantError raise lines:   [1019, 1027, 1035]
First posture_change append line:                    1078
First envelope_edit append line:                     1115

All invariant raises BEFORE posture_change append:   True
mutate BEFORE posture_change append:                 True
posture_change BEFORE envelope_edit append:          True
```

The AST walk traverses `PostureGate.request_transition`, finds the `envelope.mutate_for_posture_level(...)` call site, enumerates all `raise PostureEnvelopeMutationInvariantError(...)` statements, and locates the first `_ledger.append(entry_type="posture_change", ...)` + first `_ledger.append(entry_type="envelope_edit", ...)`. Every invariant raise (lines 1019, 1027, 1035) fires BEFORE line 1078 (the first Ledger append). The mutate call (line 1008) ALSO precedes the first append. Step 5a (1078) precedes Step 5b (1115). The atomic precondition gate is structurally in place — invariant violations CANNOT reach line 1078.

**Verdict: PASS.**

---

### R3-2 — R2-F1 Tier 1 fail-closed pin (4 invariant-check methods flipped)

**Closure claim:** Shard 3 commit `e21d67f` flipped `TestStep5bMutationInvariantChecks` methods from asserting `types == ["posture_change"]` (the prior broken pin) to asserting `types == []` (zero Ledger entries on raise).

**Fresh verification — grep against `tests/tier1/test_posture_gate_5_step_fail_closed.py` (HEAD):**

```text
$ grep -nE "assert types == \[\]" tests/tier1/test_posture_gate_5_step_fail_closed.py
1679: assert types == [],
1712: assert types == [],
1744: assert types == [],
1774: assert types == [],
```

Four invariant-check methods, all assert `types == []`:

| Line | Method                               | Invariant                                 |
| ---- | ------------------------------------ | ----------------------------------------- |
| 1679 | `test_envelope_id_mismatch_raises`   | envelope_id mismatch                      |
| 1712 | `test_new_version_regression_raises` | new_version == prior_version (no advance) |
| 1744 | `test_new_version_skip_raises`       | new_version > prior_version + 1 (skip)    |
| 1774 | `test_malformed_diff_hash_raises`    | diff_hash !~ `^sha256:[a-f0-9]{64}$`      |

Each method also asserts `sink.writes == []` (BET-12 emission is Step 5+; MUST NOT fire when precondition blocks Step 5a).

**Verdict: PASS.**

---

### R3-3 — R2-F1 Tier 2 real-EnvoyLedger pin (3 methods, NO mocking)

**Closure claim:** Shard 3 commit `a8a4fdb` adds `TestF2InvariantViolationEmitsNeitherEntry` against real `EnvoyLedger` (per `rules/testing.md` Tier 2 NO mocking) with 3 methods asserting zero entries after each invariant violation.

**Fresh verification:**

```text
$ grep -nE "class TestF2InvariantViolationEmitsNeitherEntry|async def test_" tests/tier2/test_posture_gate_wiring.py | head -10
672: class TestF2InvariantViolationEmitsNeitherEntry:
701: async def test_envelope_id_mismatch_emits_neither_entry
747: async def test_new_version_drift_emits_neither_entry
789: async def test_malformed_diff_hash_emits_neither_entry
```

```text
$ grep -nE "MagicMock|@patch|unittest.mock|from unittest" tests/tier2/test_posture_gate_wiring.py
15:   <docstring disclaimer: "NO mocking (@patch, MagicMock, unittest.mock — BLOCKED)">
630:  <docstring disclaimer: "this is a REAL adapter — a malicious-shape">
```

No actual mocking imports — both hits are within docstrings disclaiming the no-mock contract. Tests use `envoy_ledger: EnvoyLedger` fixture directly.

Each of the 3 methods:

| Line | Method                                          | Invariant violation  | Assertion       |
| ---- | ----------------------------------------------- | -------------------- | --------------- |
| 701  | `test_envelope_id_mismatch_emits_neither_entry` | envelope_id mismatch | `entries == []` |
| 747  | `test_new_version_drift_emits_neither_entry`    | new_version skip     | `entries == []` |
| 789  | `test_malformed_diff_hash_emits_neither_entry`  | diff_hash malformed  | `entries == []` |

`_read_appended_entries(envoy_ledger)` reads real append rows from the live `EnvoyLedger` fixture; the assertion exercises the FFI/persistence path, not the in-memory fake.

**Verdict: PASS.**

---

### R3-4 — R2-F2 Protocol side-effect-free contract pin

**Round 2 finding (MED):** `_PostureCarryingEnvelope` Protocol docstring didn't formally pin side-effect-free attribute reads as the implementor contract, and no test pinned the gate's attribute-read surface bound.

**Closure claim:** Shard 3 commit `eb72777` adds a "Side-effect-free attribute reads (R2-F2 contract — IMPLEMENTORS)" section to the Protocol docstring + adds `TestPostureCarryingEnvelopeProtocolDiscipline` spy-adapter test pinning attribute-read surface bound.

**Fresh verification:**

```text
$ grep -n "side-effect-free\|Side-effect-free\|R2-F2 contract" envoy/authorship/posture_gate.py
675: Side-effect-free attribute reads (R2-F2 contract — IMPLEMENTORS):
677: Implementations of this Protocol MUST have side-effect-free attribute
693: The gate cannot enforce side-effect-freeness at runtime (Python's

$ grep -n "TestPostureCarryingEnvelopeProtocolDiscipline" tests/tier1/test_posture_gate_5_step_fail_closed.py
1972: class TestPostureCarryingEnvelopeProtocolDiscipline:
```

The Protocol docstring (lines 671-700) now declares the side-effect-free contract for implementors and explicitly enumerates what they MUST NOT do on attribute read (I/O, lock acquisition, log emission, observable state mutation, background-task wakeup).

The test (line 1989, `test_protocol_attribute_read_surface_bounded`):

- Defines a `_SpyEnvelope` overriding `__getattribute__` to record every attribute name read.
- Calls `_is_posture_carrying_envelope(spy)`.
- Asserts the observed read surface equals `{"envelope_id", "prior_version", "prior_content_hash", "prior_posture_level", "mutate_for_posture_level"}` (exactly 5 names).
- Asserts `unexpected_reads == []` and `len(protocol_reads) == 5` (no duplicate reads, no surface growth).

A future refactor that adds spurious `hasattr()` / `getattr()` calls inflates `len(protocol_reads)` and fires the assertion.

**Verdict: PASS.**

---

### R3-5 — R2-F3 spec clarification notes (present-tense, no future-tense)

**Round 2 finding (LOW):** `specs/posture-ladder.md::effective_posture_for_composition` + `specs/shared-household.md::compose_cross_principal_action` reference `p.posture_level` ambiguously — could be misread as the envelope's `metadata.posture_level` mint-state field (which is audit-only per F-4 disposition) vs. the per-principal current-effective-posture derived from the Ledger.

**Closure claim:** Shard 3 commit `58a3b78` adds clarification notes at both call sites citing `specs/envelope-model.md § metadata.posture_level`, present-tense only (no "Pre-Phase-03 / will be refactored" framings per `rules/spec-accuracy.md`).

**Fresh verification — diff inspection:**

```diff
+++ specs/posture-ladder.md (post-Shard 3, line 128)
+    # `p.posture_level` here is the PRINCIPAL'S current effective posture
+    # (derived per principal by walking that principal's Ledger
+    # `posture_change` entries — the audit-chain authority). It is NOT the
+    # envelope's `metadata.posture_level` field, which is the mint-time
+    # annotation per specs/envelope-model.md § metadata.posture_level.

+++ specs/shared-household.md (post-Shard 3, line 102)
+    # `p.posture_level` is the principal's current effective posture derived
+    # from that principal's Ledger `posture_change` chain — NOT any envelope's
+    # mint-time `metadata.posture_level` annotation (per
+    # specs/envelope-model.md § metadata.posture_level).
```

**Present-tense check (per `rules/spec-accuracy.md`):**

```text
$ git diff e89914b..2264ae2 -- specs/posture-ladder.md specs/shared-household.md | grep -iE "will be|phase[-_ ]?0?3|pre-phase|todo|tbd|fixme|future|scaffold|promised|target|pending|to be wired|backend follow|fe follow"
[empty — no BLOCKED phrasings introduced]
```

Both clarification notes:

- Cite `specs/envelope-model.md § metadata.posture_level` as the audit-only-role spec (F-4 closure landed in Round 1 Shard 1).
- Use present tense ("is", "is derived", "is the mint-time annotation").
- Contain no future-tense planning, no work-tracker markers, no split-state framings.

**Note:** A pre-existing `TBD` exists at `specs/shared-household.md:165` (introduced in Phase-00 commit `3c496f9`, predating T-02-33). Confirmed NOT introduced by Shard 3 (`git diff e89914b..2264ae2 -- specs/shared-household.md | grep TBD` empty). Outside Shard 3 scope per `rules/zero-tolerance.md` Rule 1c (existence pre-dates the session's first tool call — verifiable from git log).

**Verdict: PASS.**

---

## Section 2 — Test-coverage re-derivation (no .test-results trust)

### R3-6 — pytest collect-only exit 0 + total count

```text
$ .venv/bin/pytest --collect-only -q tests/
... [609 entries]
609 tests collected in 0.45s
exit code: 0
```

**Verdict: PASS — 609 tests collected.**

### R3-7 — Tier 1 posture gate suite

```text
$ .venv/bin/pytest tests/tier1/test_posture_gate_5_step_fail_closed.py -x -q --tb=short
110 passed in 0.05s
```

**Verdict: PASS — 110/110.**

### R3-8 — Tier 2 posture gate wiring

```text
$ .venv/bin/pytest tests/tier2/test_posture_gate_wiring.py -x -q --tb=short
14 passed in 0.03s
```

**Verdict: PASS — 14/14.**

### R3-9 — Tier 2 envelope-hash-mint-time-cached

```text
$ .venv/bin/pytest tests/tier2/test_envelope_hash_mint_time_cached.py -x -q --tb=short
6 passed in 0.01s
```

**Verdict: PASS — 6/6.**

### R3-10 — Full suite

```text
$ .venv/bin/pytest tests/ -x -q --tb=short
609 passed in 17.65s
```

**Verdict: PASS — 609/609.**

---

## Section 3 — New-finding sweeps (mechanical)

### R3-11 — Cross-CLI artifact hygiene (`rules/cross-cli-artifact-hygiene.md`)

```text
$ grep -rEn "Agent\(subagent_type=|run_in_background=true|TaskCreate|TaskUpdate|isolation=\"worktree\"|per CLAUDE\.md|CLAUDE\.md is the authority|the Read tool|the Bash tool|the Write tool|the Edit tool|SessionStart hook|PreToolUse|PostToolUse|UserPromptSubmit" workspaces/phase-01-mvp/journal/0024-DECISION-precondition-invariants-and-orphan-prevention.md
[empty]
```

**Verdict: PASS — journal 0024 is CLI-neutral.**

### R3-12 — Spec-accuracy compliance (`rules/spec-accuracy.md`)

```text
$ git diff e89914b..2264ae2 -- specs/posture-ladder.md specs/shared-household.md | grep -iE "will be|phase[-_ ]?0?3|pre-phase|todo|tbd|fixme|future|scaffold|promised|target|pending|to be wired"
[empty]
```

**Verdict: PASS — Shard 3 spec edits introduce no BLOCKED phrasings.**

### R3-13 — Silent fallback patterns

```text
$ git diff e89914b..2264ae2 -- envoy/authorship/posture_gate.py | grep -nE "^[+].*(except.*pass|except.*continue|except Exception:|return None  # \w)"
[empty]
```

**Verdict: PASS — no `except: pass`, no silent `return None`, no bare-except.**

### R3-14 — Probe-driven discipline (`rules/probe-driven-verification.md`)

```text
$ git diff e89914b..2264ae2 -- tests/ | grep -nE "^[+].*(re\.search|re\.match|re\.findall|str\.contains)"
[empty]
```

**Verdict: PASS — no regex-on-semantic-claims in Shard 3 test additions. Tests use AST extraction (`ast.parse`), direct assertion on Ledger row shape, and structural `set(observed) == expected`.**

### R3-15 — LOC growth / refactor-invariants

```text
envoy/authorship/posture_gate.py:                    1101 -> 1151 (delta=+50)
specs/posture-ladder.md:                              212 ->  217 (delta=+5)
specs/shared-household.md:                            161 ->  165 (delta=+4)
tests/tier1/test_posture_gate_5_step_fail_closed.py: 1998 -> 2161 (delta=+163)
tests/tier2/test_posture_gate_wiring.py:              838 -> 1053 (delta=+215)
```

No file shrank. `rules/refactor-invariants.md` MUST 1 (LOC invariant test required on shrinking refactor) NOT triggered.

**Verdict: PASS.**

### R3-16 — Log triage gate (`rules/observability.md` Rule 5)

```text
$ .venv/bin/pytest tests/ -q --tb=short 2>&1 | grep -iE "warn|error|deprecat|fail" | sort -u
[empty]
```

**Verdict: PASS — full suite emits zero WARN+/ERROR/DEPRECATION entries.**

### R3-17 — Orphan check on R2-F2 spy adapter contract

The R2-F2 closure (Protocol docstring + spy test) does not introduce any new manager-shape class, facade attribute, or `*Executor` / `*Manager` / `*Store` / `*Registry` symbol. `rules/orphan-detection.md` Rules 1-2 + `rules/facade-manager-detection.md` Rule 1 not triggered. The `_is_posture_carrying_envelope` helper (Round 1 F-1 closure) already had production call site at `posture_gate.py:852` and is now also exercised by the new Tier 1 spy-adapter test.

**Verdict: PASS — no new orphans.**

### R3-18 — `__all__` consistency

No new public symbols added in Shard 3. `PostureEnvelopeMutationInvariantError` was already in `envoy/authorship/__init__.py::__all__` from Round 1 Shard 1. No `__all__` reconciliation pending (`rules/orphan-detection.md` Rule 6/6a).

**Verdict: PASS.**

---

## Section 4 — Convergence verdict

**CLEAN.** All three Round 2 findings (R2-F1 HIGH, R2-F2 MED, R2-F3 LOW) closed by Shard 3 with structural defenses verified via fresh AST + grep + pytest commands. No new findings introduced. Full suite green at 609/609.

**Closure summary:**

| Finding | Sev (R2) | Shard 3 commit                    | Verification rows | Verdict |
| ------- | -------- | --------------------------------- | ----------------- | ------- |
| R2-F1   | HIGH     | `bf1f66f` + `e21d67f` + `a8a4fdb` | R3-1, R3-2, R3-3  | PASS    |
| R2-F2   | MED      | `eb72777`                         | R3-4              | PASS    |
| R2-F3   | LOW      | `58a3b78`                         | R3-5              | PASS    |

**New-finding sweep totals:** 8 rows (R3-11 through R3-18), all PASS.

**Test coverage totals:** 609 collected / 609 passing / 17.65s wall-time.
