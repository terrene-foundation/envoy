# Round 4 — Convergence Confirmation (consecutive clean reproduction)

**Audit date:** 2026-05-24
**Round:** 4 of /redteam (second of 2 consecutive clean rounds — convergence run)
**Same SHA as Round 3:** `2264ae2`
**Diff scope (cumulative T-02-33 + Shards 1-3):** `git diff 641dd2d..2264ae2`
**Methodology:** per `skills/spec-compliance/SKILL.md` § Audit Protocol + `rules/probe-driven-verification.md` § Structural-Probes-Required + `rules/testing.md` § Audit Mode Rules. Verification re-derived from scratch (NO trust of Round 3 outputs); probes are deterministic AST/grep/pytest so identical inputs against identical SHA MUST produce identical outputs — any divergence is a HIGH non-determinism finding.

---

## Section 1 — Round 3 probe stability check

### R4-1 — R3-1 reproduction: AST atomicity ordering

Probe: AST walk over `envoy/authorship/posture_gate.py::PostureGate.request_transition`, enumerate `mutate_for_posture_level` call line, all `PostureEnvelopeMutationInvariantError` raise lines, first `posture_change` append line, first `envelope_edit` append line.

```text
mutate_for_posture_level call line:                  1008
PostureEnvelopeMutationInvariantError raise lines:   [1019, 1027, 1035]
First posture_change append line:                    1078
First envelope_edit append line:                     1115

All invariant raises BEFORE posture_change append:   True
mutate BEFORE posture_change append:                 True
posture_change BEFORE envelope_edit append:          True
```

**Round 3 baseline:** lines 1008 / [1019, 1027, 1035] / 1078 / 1115. **Round 4 reproduction:** identical.

**Verdict: PASS — deterministic AST probe reproduces.**

---

### R4-2 — R3-2 reproduction: Tier 1 `types == []` (4 invariant-check methods)

Probe: grep `assert types == \[\]` against `tests/tier1/test_posture_gate_5_step_fail_closed.py`.

```text
1679:        assert types == [], (
1712:        assert types == [], (
1745:        assert types == [], (
1775:        assert types == [], (
```

**Round 3 baseline:** lines 1679, 1712, 1744, 1775 (Round 3 doc had a one-digit typo on the third line — actual grep output 1745, doc transcribed 1744; probe answer of "4 methods all assert `types == []`" remains correct).

**Round 4 reproduction:** 4 hits, lines 1679 / 1712 / 1745 / 1775.

**Verdict: PASS — 4 methods pin `types == []`. Round 3 transcription typo on line number does not affect the structural claim (4 methods, all flipped to empty-list assertion).**

---

### R4-3 — R3-3 reproduction: Tier 2 real-EnvoyLedger `TestF2InvariantViolationEmitsNeitherEntry`

Probe A: grep class + 3 method headers in `tests/tier2/test_posture_gate_wiring.py`.

```text
672: class TestF2InvariantViolationEmitsNeitherEntry:
701:     async def test_envelope_id_mismatch_emits_neither_entry
747:     async def test_new_version_drift_emits_neither_entry
789:     async def test_malformed_diff_hash_emits_neither_entry
```

Probe B: stricter no-mocking grep — `^import (unittest\.mock|mock)$` / `^from (unittest\.mock|mock) import` / `MagicMock\(\)` / `@patch\(`:

```text
[empty — zero actual mocking imports or decorators]
```

**Round 3 baseline:** identical lines + zero actual mocking. **Round 4 reproduction:** identical.

**Verdict: PASS — 3 methods on real `EnvoyLedger` fixture, NO mocking per `rules/testing.md` Tier 2.**

---

### R4-4 — R3-4 reproduction: Protocol docstring + spy adapter test

Probe A: grep "side-effect-free" / "Side-effect-free" / "R2-F2 contract" in `envoy/authorship/posture_gate.py`:

```text
675:    Side-effect-free attribute reads (R2-F2 contract — IMPLEMENTORS):
677:    Implementations of this Protocol MUST have side-effect-free attribute
693:    The gate cannot enforce side-effect-freeness at runtime (Python's
```

Probe B: grep `TestPostureCarryingEnvelopeProtocolDiscipline` + `test_protocol_attribute_read_surface_bounded` in Tier 1:

```text
1972: class TestPostureCarryingEnvelopeProtocolDiscipline:
1989:     def test_protocol_attribute_read_surface_bounded(self):
```

**Round 3 baseline:** identical. **Round 4 reproduction:** identical.

**Verdict: PASS — Protocol docstring contract + spy adapter pin both present.**

---

### R4-5 — R3-5 reproduction: spec clarification notes (present-tense)

Probe A: grep clarification-note anchor text in `specs/posture-ladder.md` + `specs/shared-household.md`:

```text
specs/shared-household.md:102:    # `p.posture_level` is the principal's current effective posture derived
specs/shared-household.md:105:    # specs/envelope-model.md § metadata.posture_level).
specs/posture-ladder.md:128:    # `p.posture_level` here is the PRINCIPAL'S current effective posture
specs/posture-ladder.md:132:    # annotation per specs/envelope-model.md § metadata.posture_level.
```

Probe B (refined from Round 3 — restricted to ADDED lines only): `git diff e89914b..2264ae2 -- specs/posture-ladder.md specs/shared-household.md | grep -E '^[+]' | grep -iE 'will be|phase[-_ ]?0?3|...'`:

```text
[empty — no BLOCKED phrasings on added lines]
```

**Round 3 detail (preserved for audit trail):** Round 3's broader grep returned one hit which was the diff hunk header `@@ -125,6 +125,11 @@ def posture_change(current: PostureLevel, target: PostureLevel, evidence: Postur` — false positive on the literal token `target` (a function parameter name in spec pseudocode, not future-tense planning). Round 3 documented the result as `[empty]` after manual triage. Round 4 refines the probe to grep only `^[+]` lines, eliminating the false-positive class structurally.

**Verdict: PASS — clarification notes present at both call sites; refined ADDED-lines-only probe is empty.**

---

## Section 2 — Probe-stability summary

| Round 3 row | Round 4 reproduction | Same input | Same output | Probe class                 |
| ----------- | -------------------- | ---------- | ----------- | --------------------------- |
| R3-1        | R4-1                 | yes        | yes         | AST extraction              |
| R3-2        | R4-2                 | yes        | yes\*       | grep (regex)                |
| R3-3        | R4-3                 | yes        | yes         | grep + sharper no-mock grep |
| R3-4        | R4-4                 | yes        | yes         | grep (regex)                |
| R3-5        | R4-5                 | yes        | yes\*\*     | grep + refined ADDED-lines  |

\* R3-2 had a one-digit transcription typo (1744 vs actual 1745); structural claim (4 methods, all `types == []`) unchanged.
\*\* R3-5 had a false-positive hunk-header match in the original grep; Round 3 documented `[empty]` after manual triage; Round 4 used a refined probe that structurally eliminates the false positive.

**No probe produced a different STRUCTURAL answer in Round 4 vs Round 3. The two recorded deltas are transcription-only (Round 3 typo, Round 4 sharpened probe) — neither indicates non-determinism. Per `rules/probe-driven-verification.md` MUST-3 the probes are deterministic file/grep/AST checks; deterministic input + deterministic probe + same SHA = same output.**

---

## Section 3 — Test-coverage re-derivation

### R4-6 — pytest collect-only

```text
$ .venv/bin/pytest --collect-only -q tests/
...
609 tests collected in 0.39s
exit code: 0
```

**Round 3 baseline:** 609 / 0.45s. **Round 4:** 609 / 0.39s. Wall-time variance is OS-level (filesystem cache state); collected count is identical.

**Verdict: PASS — 609 tests collected.**

### R4-7 — Full suite

```text
$ .venv/bin/pytest tests/ -x -q --tb=short
609 passed in 18.10s
```

**Round 3 baseline:** 609 passed in 17.65s. **Round 4:** 609 passed in 18.10s. Wall-time variance ~3% (OS / runner state). All 609 green.

**Verdict: PASS — 609/609.**

---

## Section 4 — New-finding sweeps (fresh attention)

### R4-8 — Cumulative cross-CLI hygiene (journals 0021-0024)

```text
$ grep -rEn "Agent\(subagent_type=|run_in_background=true|TaskCreate|TaskUpdate|per CLAUDE\.md|the Read tool|the Bash tool|the Write tool|the Edit tool|SessionStart hook" \
    workspaces/phase-01-mvp/journal/0021*.md \
    workspaces/phase-01-mvp/journal/0022*.md \
    workspaces/phase-01-mvp/journal/0023*.md \
    workspaces/phase-01-mvp/journal/0024*.md
exit=1   # no matches
```

**Verdict: PASS — all four T-02-33 journals are CLI-neutral per `rules/cross-cli-artifact-hygiene.md`.**

### R4-9 — Cumulative silent-fallback sweep (envoy/)

```text
$ git diff 641dd2d..2264ae2 -- envoy/ | grep -nE "^[+].*(except.*pass|except.*continue|except Exception:\s*$|return None  # not impl|^[+]\s*pass\s*#\s*placeholder)"
[empty]
```

**Verdict: PASS — no silent-fallback patterns introduced cumulatively across T-02-33 + 3 shards.**

### R4-10 — Cumulative inline-DDL sweep (envoy/)

```text
$ git diff 641dd2d..2264ae2 -- envoy/ | grep -nE "^[+].*(CREATE TABLE|CREATE INDEX|ALTER TABLE|DROP TABLE|inline DDL)"
[empty]
```

**Verdict: PASS — no inline DDL introduced cumulatively.**

### R4-11 — Cumulative `__all__` addition sweep (orphan-export risk)

```text
$ git diff 641dd2d..2264ae2 -- envoy/ | grep -nE "^[+]__all__"
[empty]
```

Note: `PostureEnvelopeMutationInvariantError` was added to `envoy/authorship/__init__.py::__all__` cumulatively but the grep above only catches NEW `__all__ = [...]` assignment lines, not single-entry additions inside an existing list. The class is exercised by production call site at `posture_gate.py` (raised in 3 sites) AND by Tier 1 tests (4 invariant-check methods) AND by Tier 2 tests (3 methods) per Round 3 R3-17. Not orphaned.

**Verdict: PASS — no new `__all__` blocks introduced; existing additions exercised by production + tests.**

### R4-12 — Log triage gate

```text
$ .venv/bin/pytest tests/ -q --tb=short 2>&1 | grep -iE "warn|error|deprecat|fail" | sort -u
[empty]
```

**Verdict: PASS — full suite emits zero WARN+/ERROR/DEPRECATION entries cumulatively.**

### R4-13 — Working-tree state

```text
$ git status --porcelain
?? workspaces/phase-01-mvp/.spec-coverage-round-2-2026-05-24.md
?? workspaces/phase-01-mvp/.spec-coverage-round-3-2026-05-24.md
?? workspaces/phase-01-mvp/04-validate/round-2-code-review-2026-05-24.md
?? workspaces/phase-01-mvp/04-validate/round-2-security-audit-2026-05-24.md
?? workspaces/phase-01-mvp/04-validate/round-2-spec-compliance-2026-05-24.md
?? workspaces/phase-01-mvp/04-validate/round-3-code-review-2026-05-24.md
?? workspaces/phase-01-mvp/04-validate/round-3-security-audit-2026-05-24.md
?? workspaces/phase-01-mvp/04-validate/round-3-spec-compliance-2026-05-24.md
```

Only untracked redteam artifacts (workspace bookkeeping, additive). No modified tracked files. SHA unchanged from `2264ae2`.

**Verdict: PASS.**

---

## Section 5 — Convergence verdict

**CLEAN.** All five Round 3 probes reproduce structurally identically against the same SHA `2264ae2`. The two recorded deltas (R4-2 line-number typo; R4-5 false-positive hunk-header) are transcription-only and do not indicate non-determinism — the underlying structural claims (4 methods pin `types == []`; clarification notes present-tense only) hold under sharpened probes.

| Round 3 finding-closure row | Round 4 reproduction | Verdict |
| --------------------------- | -------------------- | ------- |
| R3-1 (AST atomicity)        | R4-1                 | PASS    |
| R3-2 (Tier 1 fail-closed)   | R4-2                 | PASS    |
| R3-3 (Tier 2 real-Ledger)   | R4-3                 | PASS    |
| R3-4 (Protocol + spy)       | R4-4                 | PASS    |
| R3-5 (spec clarifications)  | R4-5                 | PASS    |

**Cumulative sweep totals (8 new rows R4-6 through R4-13):** all PASS.

**Test coverage:** 609 collected / 609 passing / 18.10s wall-time / zero WARN+.

**Probe-stability check (per task Step 3):** all 5 Round 3 closure probes produced the same structural answers in Round 4. No HIGH non-determinism finding.

**Convergence criterion met: 2 consecutive clean rounds (Round 3 + Round 4).**
