# Session Notes — 2026-06-01

## Where we are

The durable-ledger-export workstream (EC-4/EC-9) is COMPLETE: A→B1→B2→B3→C all
merged to main. `envoy ledger export` ships; canonical CLI surface is now 7/10.
Phase: implement. Next: the F2 independent verifier (separate repo) consumes the
bundle, then F4 (EC-6 redteam) → Phase-01 ship.

## Read first

1. `journal/0049-DECISION-c-ledger-export-delivered-7of10.md` — C delivery + 6/10→7/10 re-scope + 3 open "For Discussion".
2. `specs/mvp-build-sequence.md` Milestone-5 (~:190) + Phase-02 hooks item 9 — ship gate + remaining 3 deferred (`init`/`chat`/`grant`).
3. `envoy/cli/ledger.py` — the `envoy ledger export` producer (F2 consumes its bundle).
4. `envoy/ledger/bootstrap.py` — shared ledger identity (`LEDGER_*` consts) + `open_durable_ledger`.
5. `specs/independent-verifier.md` — the F2 bundle wire-format contract.

## In-flight state

- `uv.lock` has an uncommitted benign `envoy-agent` 0.0.0→0.1.0 sync (not committed this session).

## Outstanding ledger (forest)

| ID | Item | Value-anchor (MUST-1) | Status |
| -- | ---- | --------------------- | ------ |
| F2 | Independent ledger verifier (`envoy-ledger-verifier`) | 02-mvp-objectives EC-9 | in-flight (separate repo); producer now DELIVERED — can consume a real `envoy ledger export` bundle |
| F4 | EC-6 full-Phase-01 redteam (2 clean rounds) | 02-mvp-objectives EC-6 | BLOCKED on F2 |
| F5.2-grant | `envoy grant` | specs/mvp-build-sequence:128 | BLOCKED — needs pending-grant TrustVault sub-store + SessionRouter (durable-store first-unblock landed; grant substrate unbuilt — journal/0049) |
| F21 | Onboarding arc (`init`/`chat`) | EC-1/EC-7 + specs:128 | BLOCKED — BC bootstrap + prod CommitmentBinder |
| F20 | Web visible-secret render (client WS modal) | grant-moment §Rendering | BLOCKED on Wave-4 Nexus InboundRouter |
| F5.3 | pipx Windows smoke | build-sequence §M5 | queued |
| F6 | Redteam follow-up tests (F6/F8/F9/F11/F19) | EC-4/5(d)/7/8 | queued |
| F7 | Phase-02/Wave-2/3 deferred bucket | journal/0043,0046,0047 | VERIFIED-deferred |

Closed this session: `F5.2-ledger` → receipts PR #70 (B3) + PR #71 (C); journal 0049 (A #67, B1 #68, B2 #69 also merged this session).

## Traps

- `LEDGER_SIGNING_KEY_ID` / `LEDGER_DEVICE_ID` string VALUES are load-bearing — existing on-disk ledgers + keychain entries are keyed by them; changing a value orphans them (`envoy/ledger/bootstrap.py`).
- Wiring a new CLI subcommand MUST add it to `REGISTERED_AS_OF_F5` in `tests/e2e/test_envoy_cli_packaging_acceptance.py` in the SAME PR (strict-xfail tripwire) — and run `tests/e2e/`, not just tier1+tier2+regression.
- CI gate (`.github/workflows/tests.yml`): mypy + pyright + full `pytest tests/` + `--cov-fail-under=90` on py3.11+3.13. `tests-gate` is HARD-REQUIRED on main via ruleset "Require tests-gate on main" (id 17145354, empty bypass) — `--admin` does NOT bypass CI now; code PRs need tests-gate green to merge (doc-only PRs pass the sentinel instantly). Classic review req still admin-bypassable (solo-merge preserved). Local parity: `uv run mypy/pyright envoy/` (pyright now a pinned dev dep). IDE/LS "could not resolve envoy.ledger.*" is FALSE — trust CLI/CI.
- CLI tests are SYNC (`CliRunner`); hermetic keychain via `keyring.set_keyring(in-mem)`. Bootstrap tests inject `keyring_backend=`.

## Open questions for the human

- journal/0049 "For Discussion": (1) does Phase-01 ship at 7/10 with the producer done but F2 (separate repo) not yet consuming a real bundle? (2) commit a producer-side conformance fixture before F2 builds against `envoy-ledger-export/1.0`?
