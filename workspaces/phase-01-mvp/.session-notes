# Session Notes — 2026-05-28

## Where we are

F5.1 (Wave-5 CLI packaging Tier-3 acceptance scaffold, PR #49 → `3bcfa13`) merged on main. Working tree clean. Phase-01 in-repo critical path now narrows to F5.2 (shard-19 Wave-5 build wiring 9 unwired Milestone-5 subcommands) and the F2-blocked redteam at F4. Multi-operator roster still NOT enrolled — legacy single-writer `.session-notes` fallback.

## Read first

1. `tests/e2e/test_envoy_cli_packaging_acceptance.py` — F5.1 packaging-shape gate. `REGISTERED_AS_OF_F5` frozenset is the toggle; xfail list = Milestone-5 progress signal. Append a subcommand name when shard 19 wires it.
2. `workspaces/phase-01-mvp/02-plans/01-build-sequence.md` § Wave 5 (line 264) + § Milestone 5 (line 333) — canonical anchors for the 11 Milestone-5 subcommands + release-readiness convergence.
3. `tests/e2e/test_session_continuity_5_channels.py` + `tests/helpers/deterministic_llm_provider.py` — Protocol-Satisfying Deterministic Adapter; template for future EC-N tests that need to run in CI without Ollama.
4. `envoy/cli/main.py` + `pyproject.toml [project.scripts]` — F5.2 starting point; entry-point wired (`envoy = "envoy.cli:cli"`); 2 of 11 subcommands registered today.

## Outstanding ledger (forest)

| ID   | Item                                                                                                  | Value-anchor (MUST-1 source)                                                                          | Status                                                                                 |
| ---- | ----------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| F2   | EC-4 + EC-9 independent ledger verifier (`terrene-foundation/envoy-ledger-verifier`)                  | `01-analysis/02-mvp-objectives.md` EC-9 + `01-analysis/07-independent-verifier-design.md` § 3.1       | BLOCKED — separate-repo context-switch per `rules/repo-scope-discipline.md`            |
| F4   | EC-6 full-Phase-01 redteam sweep (2 consecutive clean rounds)                                         | `01-analysis/02-mvp-objectives.md` EC-6 ship gate                                                     | BLOCKED on F2; rolling rounds banked: R3 (Wave-4 digest) + R4 (EC-7+EC-8+F5.1 subscope) |
| F5.2 | Wave-5 shard 19 build (wire 9 remaining Milestone-5 subcommands: `init`/`up`/`boundaries`/`ledger`/`grant`/`posture`/`connection`/`model`/`version`) | `02-plans/01-build-sequence.md` § Wave 5 line 264 + § Milestone 5 line 333                            | queued — depends on Boundary Conversation runtime + Trust Vault + Connection Vault + Model adapter primitives landing first per Wave 5 dependency graph |
| F5.3 | Wave-5 release-time `pipx install -e .` smoke check (Option B; covers Windows packaging surface)      | `02-plans/01-build-sequence.md` § Milestone 5 line 333 (Windows x86_64 target)                        | queued — release-readiness gate, not per-PR; requires pipx on every CI runner          |
| F6   | `specs/ledger.md` enumeration update (3 budget entry types — 35 → 38)                                 | `rules/spec-accuracy.md` Rule 1 — spec count drift                                                    | queued — Rule-5b multi-session sibling sweep across 37 specs                           |
| F7   | `subscribe_threshold` non-canonical-pct boundary validation                                           | shard 12 reviewer M2 — API contract integrity                                                         | queued — Phase-02 disposition                                                          |
| F8   | EC-7 ≤2× CLI-baseline parity assertion (timing + message-count, scoped-out by de-scope #1)            | `01-analysis/02-mvp-objectives.md` EC-7 acceptance (per-channel deviation MUST stay within 2× of CLI) | queued — value-anchor: closes the EC-7 spec-side parity gate that de-scope #1 punted; Round-4 reviewer MED-1 |
| F9   | Observability assertions (caplog keys) for EC-7 + EC-8 acceptance batteries                           | `01-analysis/02-mvp-objectives.md` EC-7 acceptance line 104 ("Per-channel deviation from CLI baseline (in completion time, in message count) MUST stay within 2×") + EC-8 acceptance line 116 ("zero state-drift findings (per a cross-channel state-equivalence test that runs daily)") | queued — value-anchor: extends R1 observability surface to EC-7 + EC-8 narratives so silent runtime-log regressions surface at acceptance time. Without caplog, state-drift can persist between channels with no operator-visible signal (EC-8(a) reads zero in tests but rows-not-emitted in production). Code-health (per `rules/observability.md` Mandatory Log Points #4) is the secondary anchor. Round-4 testing-discipline F2; re-anchored per Round-5 R5-MED-1. |
| F10  | Tier-2 facade test exercising `KailashPyRuntime.trust_cascade_revoke` through the production runtime adapter (not `StubTrustRuntime`) | `01-analysis/02-mvp-objectives.md` EC-2 acceptance line 42 ("Cascade revocation is a hard constraint per CHARTER §41 + `specs/trust-lineage.md`") + EC-8(c) line 116 ("cascade revocation of a Day-1 grant correctly revokes a Day-6 child grant initiated from a different channel") | queued — value-anchor: EC-2 acceptance binds cascade revocation as a HARD constraint, and EC-8(c) extends it cross-channel. Orchestrator-only verdict (against `StubTrustRuntime`) cannot regression-lock the production-facade binding; a silent FFI / kailash-py upgrade could break the production cascade-revoke without any acceptance test failing. Code-health (per `rules/facade-manager-detection.md` MUST-1) is the secondary anchor. Round-4 reviewer MED-2; re-anchored per Round-5 R5-MED-1. |
| F11  | Migrate 8 `CREATE TABLE IF NOT EXISTS` out of `envoy/trust/store.py` to a `db/migrations/` directory  | `01-analysis/02-mvp-objectives.md` EC-5 acceptance line 80 sub-clause (d) ("Reconstruction failure produces a clear-language error message, not a binary-data dump")                                  | queued — value-anchor: schema drift in the trust store (DDL in app code diverging from a future migration's column-set) is exactly the failure mode that produces binary-data dumps instead of clear errors on Shamir reconstruction. EC-5(d) is the user-facing acceptance gate F11 defends. Code-health (per `rules/schema-migration.md` MUST Rule 1 + `rules/zero-tolerance.md` Rule 1a scanner-symmetry) is the secondary anchor. Round-4 reviewer LOW-1; re-anchored per Round-5 R5-MED-1. |

Closed this session:
- `F5.1` → PR #49 / merge `3bcfa13`.
- `Round-4 + Round-5` rolling /redteam on EC-7 + EC-8 + F5.1 delta (PRs #47 / #48 / #49) → **subscope CONVERGES** at 2-consecutive-clean rounds (R4: 0 CRIT + 0 HIGH + 4 MED + 4 LOW; R5: 0 CRIT + 0 HIGH + 1 MED + 0 LOW). Trivial MEDs closed in-shard (F1 xfail-strict, F3 spec-path, MED-1 → F8). R5-MED-1 (value-anchor citation discipline per MUST-6) re-anchored: F9/F10/F11 now cite materialized closed-allowlist sources verbatim. Remaining MED/LOW filed as value-anchored shards F8–F11. Full reports at `04-validate/round-{4,5}-*-rolling-2026-05-28.md`. Subscope convergence is ONE of the two Phase-01-ship EC-6 contributions; the OTHER (full-Phase-01 EC-6 sweep) remains gated on F2 per `rules/repo-scope-discipline.md`. PR #51.

## Traps

- F5 anchor was historically miscited as "EC-5 + EC-6" — EC-5 in `01-analysis/02-mvp-objectives.md` line 70 is Trust Vault SLIP-0039 Shamir, NOT CLI packaging. Canonical anchor is the Read-first #2 citation above. If any sibling spec or workspace doc still cites the wrong anchor, that's drift to clean.
- F5.1 resolves `.venv/bin/envoy` via `Path(__file__).resolve().parent.parent.parent` — works only for `tests/e2e/` layout; if tests are reorganized, fix the path.
- Windows packaging path (`.venv/Scripts/envoy.exe`) deliberately NOT covered by F5.1; lives in F5.3 per docstring scope-out. Don't extend F5.1 to Windows — file F5.3 instead.

## Open questions for the human

- F5.2 sequencing — pick up only after the dependency primitives (Boundary Conversation / Trust Vault / Connection Vault / Model adapter) exist, or pre-thread the scaffold now against not-yet-wired primitives?
- F5.3 priority — needed for Phase-01 ship per Milestone 5; CI-runner-pipx-availability is the structural prerequisite. Decide before Phase-01 release sequencing.
