# Session Notes — 2026-05-31

## Where we are

Phase-01-mvp `/redteam` security-hardening cycle. This session ran a cascade-revocation
deep-dive (F10→F14) + a 5-cluster security-hard-constraint stub-masking audit, fixing the
gaps that fire in Phase-01 and verifying/deferring the rest. 8 PRs merged (#51–#58); main
clean. Multi-operator roster NOT enrolled — legacy single-writer `.session-notes`.

## Read first

1. `journal/0047` then `0042`–`0046` — this session's findings/decisions (cascade engine bug,
   anti-spoofing render, audit, F16, F15-c Phase-02 verdict). Single entry point to context.
2. `01-analysis/02-mvp-objectives.md` — EC acceptance gates (closed-allowlist value-anchors).
3. `01-analysis/07-independent-verifier-design.md` §3.1/3.2/3.5 — F2 fresh-session scope.
4. `04-validate/round-{4,5}-*-2026-05-28.md` — prior rolling-convergence baseline.

## In-flight state

- Nothing uncommitted; working tree clean, all work merged to main.

## Outstanding ledger (forest)

| ID | Item | Value-anchor | Status |
| -- | ---- | ------------ | ------ |
| F2 | Independent ledger verifier (`terrene-foundation/envoy-ledger-verifier`) | `02-mvp-objectives.md` EC-9 L128 + `07-independent-verifier-design.md` §3.1 | AUTHORIZED, needs FRESH session (EC-9 "different agent") |
| F4 | EC-6 full-Phase-01 redteam sweep (2 clean rounds) | `02-mvp-objectives.md` EC-6 L92 ship gate | BLOCKED on F2 |
| F15-b.2 | Render visible secret on web/slack/telegram/discord | `specs/grant-moment.md` L80-82 (T-018) | queued — high-stakes already covered (b.1, #57); low-stakes parity |
| F5.2/F5.3 | Wave-5 CLI: 9 subcommands + pipx Windows smoke | `02-plans/01-build-sequence.md` §Wave5 L264 / §M5 L333 | queued — depends on Wave-2/3/4 primitives |
| F13 | pyright/mypy backlog (~35 pre-existing errors) | `git.md` pre-flight `mypy --strict` | queued — de-risk before EC-6 |
| F7/F12-b/F12-c/F15-c/F17/F18 | Phase-02/Wave-2/3 deferred bucket | EC-2/EC-8(c)/EC-4 + `grant-moment.md` (resources don't exist in P01) | VERIFIED-deferred (journal/0043,0046,0047), not forgotten |
| F6/F8/F9/F11/F19 | Redteam follow-up tests + schema migration | EC-4/EC-5(d)/EC-7/EC-8 + Thesis §2.3 | queued — bounded P01 tests |

Closed this session: `F10`→PR #52; `F12-a`+`F14`→PR #54; `F15-a`→PR #56; `F15-b.1`→PR #57; `F16`→PR #58.

## Traps

- **F2 MUST run from a FRESH session** — EC-9 requires "different agent without reference to
  producer source"; any producer-touching session is contaminated (journal/0042). journal/0047
  suggests F2 reuse F16's tamper battery (`tests/tier2/test_envoy_ledger_tamper_detection.py`).
- Phase-02 deferrals (F7/F12-b/F12-c/F15-c/F17/F18) are VERIFIED-deferred (the substrate doesn't
  exist in Phase-01) — do NOT "fix" them in P01; building renders for non-existent data is orphan work.
- ~35 pre-existing mypy errors (e.g. runtime.py:906/923) predate this work; `mypy --strict` not clean.

## Open questions for the human

- F2: authorize a fresh session for the Python verifier (REQUIRED for EC-9) — or Rust variant first?
- F6 still lacks a primary user-anchored source (`value-prioritization.md` MUST-4 gate) — surface a spec § or close-with-gate.
