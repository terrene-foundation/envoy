# Session Notes — 2026-05-28

## Where we are

Shard 12 Budget tracker (PR #45) + COC sync 2.22.3 → 2.23.1 (PR #46) both landed on main (HEAD `1dd0ddb`). EC-8(b) double-billing guard in place via shipped Budget primitive; the rest of EC-8 (cross-channel state-equivalence + cascade + 7-day coherence) is the next pick. No open PRs.

## Read first

1. `workspaces/phase-01-mvp/journal/0041-DISCOVERY-shard-12-budget-tracker-gate-review-closures.md` — shard 12 convergence + 2 patterns + receipts
2. `workspaces/phase-01-mvp/02-plans/02-test-strategy.md` § EC-8 (lines 246–279) — the 4 acceptance tests, all now buildable
3. `specs/budget-tracker.md` § Ledger entries emitted + § Test location — shipped budget contract
4. `envoy/budget/orchestrator.py` (~lines 314–360) — record-intent-first guard timing (no double-CHARGE / no double-RELEASE)
5. `.claude/.coc-sync-marker` — sync at 2.23.1 (loom 2.35.0); new rules + multi-operator commands available

## In-flight state

- Multi-operator roster: this clone is **NOT enrolled**. New surface (`/onboard /certify /claim /claims /release-claim /whoami`) is available but `L2_SUPERVISED` until `/whoami --register` lands. The split-layout `<base>/.session-notes.d/<display_id>.md` (per `rules/knowledge-convergence.md` MUST-1) is therefore NOT in effect — this file is the legacy single-writer fallback.

## Outstanding ledger (forest)

| ID  | Item                                                                                                  | Value-anchor (MUST-1 source)                                                                             | Status                                                                                  |
| --- | ----------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| F1  | EC-8 acceptance suite (envelope byte-identity / cross-channel cascade / no-double-billing / 7-day e2e) | `02-mvp-objectives.md` EC-8 line 116 ("channel-as-UI thesis; if broken, the thesis is at risk") + line 149 | queued — all 4 tests buildable against shipped primitives                              |
| F2  | EC-4 + EC-9 independent ledger verifier (`terrene-foundation/envoy-ledger-verifier`)                  | `02-mvp-objectives.md` EC-9 + `01-analysis/07-independent-verifier-design.md` § 3.1                       | BLOCKED — separate-repo context-switch per `rules/repo-scope-discipline.md`            |
| F3  | EC-7 5-channel session-continuity acceptance test (de-scope #1 invocation)                            | `02-mvp-objectives.md` § 5 EC-7 DEGRADE-ACCEPTABLE                                                       | queued — 5 channels shipped (CLI+Web+Telegram+Slack+Discord); acceptance test pending  |
| F4  | EC-6 full-Phase-01 redteam sweep (2 consecutive clean rounds)                                         | `02-mvp-objectives.md` EC-6 ship gate                                                                    | BLOCKED on F1+F2+F3 — last in critical path                                            |
| F5  | Wave-5 CLI packaging (`pipx install envoy-agent`)                                                     | `02-mvp-objectives.md` EC-5 + EC-6 + Milestone-5 build-sequence                                          | BLOCKED on F1 (EC-8 ∧ EC-7)                                                            |
| F6  | `specs/ledger.md` enumeration update (3 new budget entry types — 35 → 38)                              | `rules/spec-accuracy.md` Rule 1 — spec count drift                                                       | queued — deferred from shard 12 (Rule-5b sibling re-derivation is multi-session)       |
| F7  | `subscribe_threshold` non-canonical-pct boundary validation                                            | shard 12 reviewer M2 — API contract integrity                                                            | queued — not reachable through Phase-01 production; Phase-02 disposition               |

Closed this session: (initial ledger — no prior IDs; shard 12 + sync landed via PR #45 → `1dd0ddb` and PR #46 → `253d321`).

## Traps

- **Ledger is int-only** — `envoy.ledger.canonical.canonical_dumps` rejects floats. Fractional fields encode as integer basis points (precedent: `threshold_bps`).
- **`EnvoyLedger.verify_chain()` and `.query()` are async** despite signature-stub appearance — pyright catches the missing `await`.
- **`record_for_call` guard timing is record-intent-first** (set `_recorded_reservations` BEFORE `_rollback` and BEFORE the record loop). Closes both expired-replay over-release AND partial-fail double-charge. Don't move back to post-success.
- **Anomaly detector dominates small-budget tests** — inject `tests/helpers/budget_harness.no_op_anomaly_detector()` (threshold `1e18`) to isolate ceiling/threshold/isolation behavior.
- **`uv sync` alone does NOT install dev extras** — pytest can appear missing. Fix: `uv sync --all-extras`.
- **freezegun is NOT in `.venv`** — budget tests use a mutable `_Clock()` helper (daily-digest precedent).
