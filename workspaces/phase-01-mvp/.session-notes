# Session Notes — 2026-06-04

## Where we are

Phase-01 largely COMPLETE (7/10 CLI shipped). Phase: redteam→implement. Last
change: fresh full-Phase-01-surface /redteam CONVERGED at 0 CRIT/0 HIGH (PR #80
merged 563a74a; receipt journal/0053). In-repo surface is red-team-clean; the
remaining forest is externally blocked. Next move is a human unblock decision.

## Read first

1. `journal/0053-DECISION-redteam-fresh-fullsurface-convergence.md` — this cycle's verdict + deferred-item dispositions.
2. `04-validate/round-1-fresh-fullsurface-convergence-2026-06-03.md` — round report + receipts.
3. `journal/0050`–`0052` — CI gate + ResourceWarning root-cause + kailash-py#1245 (still live).
4. `specs/mvp-build-sequence.md` Milestone-5 — ship gate + remaining deferred.
5. `.claude/workflows/redteam-round.mjs` — reusable redteam workflow (see Traps re: emit limit).

## In-flight state

- Working tree clean; everything merged to main. No uncommitted state.
- `-W error::ResourceWarning` deliberately NOT enabled (blocked on F22 / kailash-py#1245).

## Outstanding ledger (forest)

| ID  | Item | Value-anchor (MUST-1) | Status |
| --- | ---- | --------------------- | ------ |
| F2  | Independent ledger verifier (`envoy-ledger-verifier`) | 02-mvp-objectives EC-9 | in-flight, SEPARATE repo (out of scope); producer DELIVERED |
| F4  | EC-6 full-Phase-01 redteam (2 clean rounds) | 02-mvp-objectives EC-6 | BLOCKED on F2 (in-repo surface now clean, journal/0053) |
| F5.2-grant | `envoy grant` | specs/mvp-build-sequence:128 | BLOCKED — pending-grant TrustVault sub-store + SessionRouter |
| F21 | Onboarding arc (`init`/`chat`) | EC-1/EC-7 + specs:128 | BLOCKED — BC bootstrap + prod CommitmentBinder |
| F20 | Web visible-secret render (client WS modal) | grant-moment §Rendering | BLOCKED on Wave-4 Nexus InboundRouter |
| F5.3 | pipx Windows smoke | build-sequence §M5 | queued (needs Windows host) |
| F6  | Redteam follow-up tests | EC-7/EC-8/BET-12 | **F8/F9/F19 DELIVERED** (journal/0054); F11 deferred-LOW (Phase-02+, 0053:32) |
| F7  | Phase-02/Wave-2/3 deferred bucket | journal 0043/0046/0047 | VERIFIED-deferred |
| F22 | ResourceWarning cleanup → enable CI `-W error` | journal 0050+0051+0052 + observability R5 | BLOCKED-UPSTREAM (kailash-py#1245) |
| F23 | Threat-coverage meta-gate (`tests/coverage/`) | threat-model.md:51 / EC-6 | DEFERRED to Phase-02 (co-owner approved 2026-06-03) |

Closed this session (2026-06-04, continue-from-0053): **F8 + F9 + F19** delivered as in-repo redteam-followup tests (journal/0054; 3 new test files, full suite 1705 passed). These were the only genuinely-actionable in-repo Phase-01 items; surfaced by an adversarial F6-reality re-verification that found 0053 never individually re-validated their anchors (MUST-3 gap). F11 stays deferred-LOW. The in-repo Phase-01 surface now has ZERO un-re-validated deferred shards.

Prior (0053 cycle): `F4` advanced (in-repo Phase-01 surface redteam-clean → journal/0053, PR #80) but stays BLOCKED on `F2`. `F23` (TEST-02 deferred).

## Traps

- CI `tests-gate` is HARD-required (ruleset): `--admin` can NOT merge over red CI. Doc-only PRs skip-pass instantly.
- ResourceWarning attribution is GC-TIMED, not allocation-site (journal/0051) — don't chase the file that "shows" it.
- `LEDGER_SIGNING_KEY_ID` / `LEDGER_DEVICE_ID` string VALUES are load-bearing (on-disk + keychain) — never change (`envoy/ledger/bootstrap.py`).
- `redteam-round.mjs` finders fail StructuredOutput emit when a dimension is too broad — keep ≤3 specs/dim + the budget/emit directive, or fall back to direct mechanical sweeps + schema-less individual Agent calls (reliable).
- The 37 specs are FROZEN full-product (all 4 phases): Test-location/`TBD` citing future paths is by-design, NOT a Phase-01 finding — do not mass-edit out-of-phase frozen specs (brief: no spec edits unless HIGH gap).
- Local parity: `uv run mypy/pyright/pytest` + run `tests/e2e/` (not just tier1/2/regression).

## Open questions for the human

- **In-repo work is now exhausted** — F6 (F8/F9/F19) delivered this session; the in-repo Phase-01 surface is complete at the 7/10 spec ceiling (mvp-build-sequence:190), red-team-clean, ZERO un-re-validated deferred shards. The remaining forest is ALL externally/strategically gated. Highest-value unblock (genuinely the human's call): prioritize the **F2 verifier** (separate repo — the last open Phase-01 deliverable per brief constraint 4 + EC-9; gates full EC-6), OR authorize **Phase-02 entry** (unblocks init/chat/grant via Phase-02 substrate — a deliberate phase boundary), OR declare Phase-01 in-repo complete and pause.
