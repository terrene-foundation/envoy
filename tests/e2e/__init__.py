"""tests/e2e — Wave-4 end-to-end acceptance tests.

EC-2 acceptance (`02-plans/02-test-strategy.md` § EC-2): 3 resolution shapes
(Approve / Decline / ApproveWithModification) execute end-to-end through the
runtime facade with cascade revocation reaching every descendant in a 3-deep
delegation tree.

Spec § Test location duress-honeypot latency parity test also lives here.
"""
